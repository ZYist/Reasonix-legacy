---
phase: 10-package-identity-unification
plan: 01
type: execute
wave: 1
autonomous: true
requirements: [ID-01, ID-02, ID-04, VER-01]
files_modified:
  - package.json
  - package-lock.json
  - src/version.ts
  - tests/version.test.ts
  - tests/cli-bundle-version-marker.test.ts
  - packages/dsnix/package.json
  - packages/dsnix/bin.cjs
  - packages/dsnix/README.md
  - .github/workflows/publish-dsnix.yml
must_haves:
  truths:
    - "The published root package is reasonix-legacy 1.3.0 and exposes exactly one reasonix-legacy executable."
    - "The dsnix workspace, shim, package graph, lock records, and dedicated publish workflow no longer exist."
    - "Runtime --version output is exactly reasonix-legacy 1.3.0 and derives its numeric version from root package metadata."
  artifacts:
    - package.json
    - package-lock.json
    - src/version.ts
    - tests/version.test.ts
  key_links:
    - "package.json version feeds src/version.ts and the generated CLI bundle marker."
    - "package.json bin and package-lock root bin agree on reasonix-legacy only."
---

<objective>
Replace the mixed `reasonix`/`dsnix`/`legacy-*` release identity with one install-time and runtime contract: npm package and CLI `reasonix-legacy` version `1.3.0`. Remove the obsolete dsnix package and publication path rather than repairing compatibility code.
</objective>

<context>
@.planning/phases/10-package-identity-unification/10-CONTEXT.md
@.planning/REQUIREMENTS.md
@package.json
@package-lock.json
@src/version.ts
@tests/version.test.ts
@tests/cli-bundle-version-marker.test.ts
@packages/dsnix/package.json
@packages/dsnix/bin.cjs
@.github/workflows/publish-dsnix.yml
</context>

<tasks>
<task type="auto">
  <name>Task 1: Establish the sole package and bin identity</name>
  <files>package.json, package-lock.json, packages/dsnix/package.json, packages/dsnix/bin.cjs, packages/dsnix/README.md, .github/workflows/publish-dsnix.yml</files>
  <action>Set root version to 1.3.0 and root bin to exactly `reasonix-legacy: dist/cli/index.js`. Delete the complete packages/dsnix directory and publish-dsnix workflow. Regenerate package-lock.json with the pinned Windows npm 11.16.0 toolchain so its root version/bin/workspace graph matches package.json and contains no dsnix package or node_modules/dsnix record. Do not rename `~/.reasonix`, internal `@reasonix/*` workspaces, historical files, tags, or remote artifacts.</action>
  <verify><automated>node -e "const p=require('./package.json'); const l=require('./package-lock.json'); if(p.name!=='reasonix-legacy'||p.version!=='1.3.0'||JSON.stringify(p.bin)!==JSON.stringify({'reasonix-legacy':'dist/cli/index.js'})) process.exit(1); if(l.packages[''].version!=='1.3.0'||JSON.stringify(l.packages[''].bin)!==JSON.stringify(p.bin)||l.packages['packages/dsnix']||l.packages['node_modules/dsnix']) process.exit(2)"</automated></verify>
  <done>Only the root reasonix-legacy package and reasonix-legacy executable remain in the current package graph.</done>
</task>

<task type="auto">
  <name>Task 2: Make runtime version identity explicit</name>
  <files>src/version.ts, tests/version.test.ts, tests/cli-bundle-version-marker.test.ts</files>
  <action>Change the display formatter and assertions from `legacy-X.Y.Z` to `reasonix-legacy X.Y.Z`, retaining the root package as numeric version authority. Update npx/install-path fixtures only where they represent the public executable/package identity; keep `.reasonix` cache/config path tests unchanged. Build and assert the generated CLI prints the exact v1.3 identity.</action>
  <verify><automated>npm run build &amp;&amp; npm test -- --run tests/version.test.ts tests/cli-bundle-version-marker.test.ts &amp;&amp; if ((node dist/cli/index.js --version).Trim() -ne 'reasonix-legacy 1.3.0') { exit 1 }</automated></verify>
  <done>The source tests, bundle marker, and built executable share the exact `reasonix-legacy 1.3.0` version line.</done>
</task>
</tasks>

<verification>
- `node -p "JSON.stringify(require('./package.json').bin)"` returns only the reasonix-legacy bin.
- `Test-Path packages/dsnix` and `Test-Path .github/workflows/publish-dsnix.yml` are both false.
- `Select-String package-lock.json -Pattern 'packages/dsnix|node_modules/dsnix|\"dsnix\"'` returns no current package-graph match.
- `npm run build` and focused version/bundle tests pass on Windows Node 24.15.0/npm 11.16.0.
</verification>

<success_criteria>
- ID-01, ID-02, ID-04, and the package/runtime portion of VER-01 are directly observable.
- There is no compatibility alias or hidden dsnix publication path.
- No historical archive/tag/release state is modified.
</success_criteria>

<output>Create `.planning/phases/10-package-identity-unification/10-01-SUMMARY.md` when done.</output>
