---
phase: 12-windows-release-automation
plan: 01
type: execute
wave: 1
autonomous: true
requirements: [WIN-01, WIN-02, WIN-03]
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/codeql.yml
  - docs/ci-branch-protection.md
  - docs/governance.md
  - CONTRIBUTING.md
  - scripts/check-docs.mjs
  - tests/package-identity.test.ts
must_haves:
  truths:
    - "CI runs only on Windows with Node.js 24.15.0, npm 11.16.0, and PowerShell while exposing install/docs/lint/typecheck/build/test gates."
    - "CI, CodeQL, and maintained branch-guidance docs refer only to the real `v1` default branch and `dev` development branch."
    - "Automated guards fail if workflows or maintained docs drift back to `main`, Ubuntu/Node 22, or other pre-v1.3 release-baseline assumptions."
  artifacts:
    - .github/workflows/ci.yml
    - .github/workflows/codeql.yml
    - docs/ci-branch-protection.md
    - docs/governance.md
    - CONTRIBUTING.md
    - scripts/check-docs.mjs
    - tests/package-identity.test.ts
  key_links:
    - "docs/ci-branch-protection.md and docs/governance.md must describe the same `v1` / `dev` branch model encoded in the workflows."
    - "scripts/check-docs.mjs and tests/package-identity.test.ts must both enforce the Windows release baseline facts rather than only documenting them."
---

<objective>
Replace the historical CI/CodeQL branch and runtime assumptions with the actual maintained release contract: Windows-only workflows, pinned Node/npm/PowerShell baseline, and consistent `v1` + `dev` guidance across automation and maintained docs.
</objective>

<context>
@.planning/phases/12-windows-release-automation/12-CONTEXT.md
@.planning/REQUIREMENTS.md
@.github/workflows/ci.yml
@.github/workflows/codeql.yml
@docs/ci-branch-protection.md
@docs/governance.md
@CONTRIBUTING.md
@scripts/check-docs.mjs
@tests/package-identity.test.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Align CI to the Windows v1.3 baseline</name>
  <files>.github/workflows/ci.yml</files>
  <action>Change CI triggers from `main`/historical matrix assumptions to the maintained `v1` + `dev` branch model. Run the workflow only on `windows-latest`, pin Node.js `24.15.0` and npm `11.16.0`, make PowerShell explicit, and keep the install, docs, lint, typecheck, build, test, coverage-summary, and benchmark dry-run gates visible in the job.</action>
  <verify><automated>node -e "const fs=require('fs'); const t=fs.readFileSync('.github/workflows/ci.yml','utf8'); if(!/branches:\s*\[v1, dev\]/.test(t)||!/runs-on:\s*windows-latest/.test(t)||!/node-version:\s*\"24\.15\.0\"/.test(t)||!/npm@11\.16\.0/.test(t)||!/shell:\s*pwsh/.test(t)) process.exit(1)"</automated></verify>
  <done>The maintained CI workflow reflects the Windows-only release baseline and no longer names `main` or Ubuntu/Node 22 as blocking release checks.</done>
</task>

<task type="auto">
  <name>Task 2: Align CodeQL and branch-guidance docs</name>
  <files>.github/workflows/codeql.yml, docs/ci-branch-protection.md, docs/governance.md, CONTRIBUTING.md, scripts/check-docs.mjs, tests/package-identity.test.ts</files>
  <action>Update CodeQL to analyze `v1` / `dev` on the Windows release baseline, then rewrite the maintained branch/release guidance so contributors and maintainers see the same `v1` default + `dev` development model and the same manual branch-protection boundary. Extend the existing repo guards so future workflow/doc drift fails verification automatically.</action>
  <verify><automated>node scripts/check-docs.mjs &amp;&amp; npm test -- --run tests/package-identity.test.ts</automated></verify>
  <done>The security scan, branch-protection docs, governance, and contributing guide are mutually consistent and protected by automated checks.</done>
</task>
</tasks>

<verification>
- `.github/workflows/ci.yml` runs only on `windows-latest` with Node.js `24.15.0`, npm `11.16.0`, and PowerShell while keeping the required release gates visible.
- `.github/workflows/codeql.yml` targets `v1` and `dev` rather than `main` and uses the maintained Windows baseline.
- `node scripts/check-docs.mjs` and `npm test -- --run tests/package-identity.test.ts` both fail if maintained docs or workflows drift from the branch/runtime contract.
</verification>

<success_criteria>
- WIN-01, WIN-02, and WIN-03 are directly observable from workflows and maintained docs.
- No maintained workflow or guidance document refers to a nonexistent `main` branch.
- Branch protection remains clearly documented as a manual GitHub setting rather than a fake in-repo guarantee.
</success_criteria>

<output>Create `.planning/phases/12-windows-release-automation/12-01-SUMMARY.md` when done.</output>
