---
phase: 12-windows-release-automation
plan: 02
type: execute
wave: 1
autonomous: true
requirements: [PUB-01, PUB-02]
files_modified:
  - .github/workflows/publish-npm.yml
  - .github/workflows/release-mirror.yml
  - docs/governance.md
  - CONTRIBUTING.md
  - docs/ci-branch-protection.md
  - scripts/check-docs.mjs
  - tests/package-identity.test.ts
must_haves:
  truths:
    - "The only maintained publication workflow publishes the root `reasonix-legacy` package and uses the documented plain `v1.3.0` tag convention."
    - "The publish workflow reuses the Windows v1.3 release gates before `npm publish` rather than publishing from an unverified shortcut path."
    - "Desktop/Tauri release-mirror assumptions are retired and the maintained stable-release asset policy is explicitly npm-package-only."
  artifacts:
    - .github/workflows/publish-npm.yml
    - docs/governance.md
    - CONTRIBUTING.md
    - scripts/check-docs.mjs
    - tests/package-identity.test.ts
  key_links:
    - "The publish workflow's tag example and package-name guard must match package.json's current `reasonix-legacy@1.3.0` contract."
    - "Docs that describe releasing must stay aligned with the actual publish workflow and the absence of desktop/Tauri release automation."
---

<objective>
Harden the maintained npm publication path so it matches the real release contract: one root package, documented plain `v1.3.0` tags, Windows release gates before publication, and no leftover desktop/Tauri mirror workflow.
</objective>

<context>
@.planning/phases/12-windows-release-automation/12-CONTEXT.md
@.planning/REQUIREMENTS.md
@.github/workflows/publish-npm.yml
@.github/workflows/release-mirror.yml
@docs/governance.md
@CONTRIBUTING.md
@docs/ci-branch-protection.md
@scripts/check-docs.mjs
@tests/package-identity.test.ts
@package.json
</context>

<tasks>
<task type="auto">
  <name>Task 1: Rebuild the npm publish workflow around the Windows release gates</name>
  <files>.github/workflows/publish-npm.yml</files>
  <action>Keep a single root publish workflow, but move it to `windows-latest`, pin Node.js `24.15.0`, pin npm `11.16.0`, make PowerShell explicit, verify the incoming tag matches `package.json`, run the same docs/lint/typecheck/build/test release gates used by CI, and only then execute `npm publish --access public` for the root `reasonix-legacy` package.</action>
  <verify><automated>node -e "const fs=require('fs'); const t=fs.readFileSync('.github/workflows/publish-npm.yml','utf8'); if(!/runs-on:\s*windows-latest/.test(t)||!/node-version:\s*\"24\.15\.0\"/.test(t)||!/npm@11\.16\.0/.test(t)||!/v1\.3\.0/.test(t)||!/npm publish --access public/.test(t)||!/node scripts\/check-docs\.mjs/.test(t)) process.exit(1)"</automated></verify>
  <done>The publish workflow now enforces the same Windows release contract that local verification uses.</done>
</task>

<task type="auto">
  <name>Task 2: Retire release-mirror and document npm-only assets</name>
  <files>.github/workflows/release-mirror.yml, docs/governance.md, CONTRIBUTING.md, docs/ci-branch-protection.md, scripts/check-docs.mjs, tests/package-identity.test.ts</files>
  <action>Delete or retire the release-mirror workflow that still encodes desktop/Tauri updater assumptions, then update maintained release docs so they say plainly that the stable release artifact is the npm package only. Extend the doc/workflow guards so the retired mirror path or desktop/Tauri release assumptions cannot silently return.</action>
  <verify><automated>node scripts/check-docs.mjs &amp;&amp; npm test -- --run tests/package-identity.test.ts</automated></verify>
  <done>The maintained release surface is explicitly npm-only and the obsolete mirror automation no longer exists.</done>
</task>
</tasks>

<verification>
- `.github/workflows/publish-npm.yml` shows the Windows pinned release baseline, the `v1.3.0`-style tag convention, and the pre-publish release gates.
- `.github/workflows/release-mirror.yml` is absent and maintained docs no longer describe desktop/Tauri release assets as current.
- `scripts/check-docs.mjs` and `tests/package-identity.test.ts` catch regressions in publish tag examples, branch/release policy, and retired mirror assumptions.
</verification>

<success_criteria>
- PUB-01 and PUB-02 are directly observable from repository workflows and maintained docs.
- The current stable release path is clearly npm-package-only.
- No remote publish, tag, or GitHub release action is executed by this local work.
</success_criteria>

<output>Create `.planning/phases/12-windows-release-automation/12-02-SUMMARY.md` when done.</output>
