---
phase: 04-build-chain-cleanup-full-regression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .github/workflows/ci.yml
  - scripts/postinstall.mjs
  - scripts/sync-desktop-version.mjs
  - .github/workflows/release.yml
  - .claude/CLAUDE.md
autonomous: true
requirements:
  - PANEL-04
user_setup: []

must_haves:
  truths:
    - "`npm run build` exits 0 with no dependency on the removed dashboard vendor-css copy or any desktop build step"
    - "The published tarball (`npm pack`) ships no desktop artifacts and registers no postinstall hook"
    - "CI workflow step names accurately describe what they run (no stale dashboard label on the build step)"
    - "`.claude/CLAUDE.md` no longer documents the removed Rust/Tauri/desktop/dashboard surfaces as if they were live"
    - "`prepare` (simple-git-hooks) still wires pre-commit/pre-push after the postinstall removal"
  artifacts:
    - path: "scripts/postinstall.mjs"
      provides: "(deleted) no-op desktop workspace installer that shipped in the tarball"
      expectation: "path absent from git tree"
    - path: "scripts/sync-desktop-version.mjs"
      provides: "(deleted) orphan desktop version-sync script whose only consumer was release.yml"
      expectation: "path absent from git tree"
    - path: ".github/workflows/release.yml"
      provides: "(deleted) dormant Tauri desktop bundling workflow (desktop-v* tag trigger)"
      expectation: "path absent from git tree"
    - path: "package.json"
      provides: "build/files/scripts aligned to pure-CLI release"
      contains: "copy-tree-sitter-grammars.mjs"
    - path: ".github/workflows/ci.yml"
      provides: "CI step name matching the actual build command"
      contains: "Build (tsup)"
    - path: ".claude/CLAUDE.md"
      provides: "project instructions reflecting the pure-CLI reality"
      contains: "Node ≥22"
  key_links:
    - from: "package.json scripts.build"
      to: "scripts/copy-tree-sitter-grammars.mjs"
      via: "build script still chains tsup → write-cli-package-marker → copy-tree-sitter-grammars (SAFE-02 red line)"
      pattern: "tsup && node scripts/write-cli-package-marker.mjs && node scripts/copy-tree-sitter-grammars.mjs"
    - from: "package.json scripts.prepare"
      to: "simple-git-hooks"
      via: "prepare (not postinstall) wires git hooks — must survive the postinstall removal"
      pattern: "\"prepare\": \"simple-git-hooks"
---

<!-- planner-discipline-allow: Tauri -->
<!-- planner-discipline-allow: rusqlite -->
<!-- planner-discipline-allow: src/server -->
<!-- planner-discipline-allow: desktop/src-tauri -->
<!-- planner-discipline-allow: postinstall -->
<!-- planner-discipline-allow: Build (tsup + dashboard) -->

<objective>
**As a** reasonix-legacy maintainer, **I want to** retire every dormant desktop/dashboard build-chain remnant so that **the pure-CLI release pipeline is self-contained** — the package ships without desktop artifacts, CI step names match reality, and the project instructions stop describing surfaces that Phases 1–3 already deleted.

This plan delivers Success Criterion 1 of Phase 4 (PANEL-04): the build chain no longer depends on or references dashboard/desktop, and the CLI builds and packs standalone. Per the MVP vertical-slice contract for this infra phase, after this plan completes `npm run build` still exits 0 and the published tarball is clean — the pure-CLI path stays usable.

Purpose: PANEL-04 locked a full-sweep cleanup (D-02). Phases 1–3 removed the runtime code (`dashboard/`, `desktop/`, `src/server/`, `desktop.ts`) but left their traces in `package.json` (`scripts.postinstall` + `files` entry), a dormant Tauri bundling workflow, two orphan scripts, one stale CI step name, and stale `CLAUDE.md` sections that mislead every downstream agent.
Output: three deleted paths, two config edits, one doc trim.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-build-chain-cleanup-full-regression/04-CONTEXT.md

# Live state already verified by planner (read these before editing):
@package.json
@.github/workflows/ci.yml
@.github/workflows/release.yml
@scripts/postinstall.mjs
@scripts/sync-desktop-version.mjs
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dormant build artifacts and sync package.json + ci.yml (per D-02)</name>
  <files>package.json, .github/workflows/ci.yml, scripts/postinstall.mjs (delete), scripts/sync-desktop-version.mjs (delete), .github/workflows/release.yml (delete)</files>
  <read_first>
    - `package.json` — confirm `scripts.postinstall` (line 31) = `node scripts/postinstall.mjs`, `files` array (lines 19-26) contains `"scripts/postinstall.mjs"`, `scripts.build` (line 32) = `tsup && node scripts/write-cli-package-marker.mjs && node scripts/copy-tree-sitter-grammars.mjs` (already dashboard-free), `scripts.prepare` (line 44) = `simple-git-hooks || true` (independent of postinstall), `workspaces` (lines 27-29) = `["packages/*"]` (already desktop-free).
    - `scripts/postinstall.mjs` — 9-line file; confirm it is a no-op desktop workspace installer whose guard is `if (!existsSync("desktop/package.json")) process.exit(0)` and therefore already does nothing (no `desktop/` dir on disk).
    - `scripts/sync-desktop-version.mjs` — confirm it writes only `desktop/src-tauri/tauri.conf.json`, `desktop/src-tauri/Cargo.toml`, `desktop/package.json` (all removed); sole consumer is `.github/workflows/release.yml`.
    - `.github/workflows/release.yml` — confirm trigger is `tags: ["desktop-v*"]` only (+ workflow_dispatch), matrix is Rust targets, all steps are desktop/Tauri bundling. Desktop-only = safe to delete.
    - `.github/workflows/ci.yml` — confirm the only stale ref is the step name `Build (tsup + dashboard)` at the step that runs `npm run build`; the matrix (ubuntu/windows + node 22) has NO Rust, and no other dashboard/desktop references exist in this file.
    - `.github/workflows/release-mirror.yml` — confirm it triggers on `release: [published]` for ALL releases and its `latest.json` rewrite step is documented "No-op when latest.json is absent (CLI-only releases)". DETERMINATION (D-02 discretion): RETAIN release-mirror.yml — it mirrors CLI releases too, not desktop-only. Do NOT delete it.
    - `.github/workflows/publish-npm.yml` + `publish-dsnix.yml` — confirm these publish the CLI binaries; RETAIN (red line).
  </read_first>
  <action>
    Execute the D-02 full-sweep build-chain retirement:

    1. `git rm scripts/postinstall.mjs` — delete the no-op desktop workspace installer (it ships in the tarball via the `files` array but does nothing without `desktop/package.json`). Per D-02, removing it is the literal fulfillment of "CLI 独立构建发布".

    2. `git rm scripts/sync-desktop-version.mjs` — delete the orphan desktop version-sync script. Its only consumer (`release.yml`) is being deleted in the same task.

    3. `git rm .github/workflows/release.yml` — delete the dormant Tauri desktop bundling workflow. Per D-02 rationale: it only fires on `desktop-v*` tags (no `desktop/` to bundle), and its removal takes the Rust matrix + R2/GitHub updater endpoints + Apple/Windows code-signing secret workflow out of the repo (a security surface reduction, see threat model). `publish-npm.yml`/`publish-dsnix.yml`/`release-mirror.yml` are RETAINED.

    4. Edit `package.json` (scoped): remove the `"postinstall"` key from `scripts` (line 31) AND remove the `"scripts/postinstall.mjs"` entry from the `files` array (line 23). Leave `scripts.build`, `scripts.prepare`, `scripts.verify`, `workspaces`, `bin`, and every other field untouched. Do NOT touch `scripts.prepare` — it is the simple-git-hooks wiring (pre-commit/pre-push) and is independent of postinstall.

    5. Edit `.github/workflows/ci.yml` (scoped): rename the step currently named `Build (tsup + dashboard)` (the step whose run line is `npm run build`) to `Build (tsup)`. Leave the rest of ci.yml (matrix, lint/typecheck/test steps, tau-bench dry-run, Discord notify) unchanged. There is no Rust matrix and no other dashboard ref in this file — a single step-name edit completes it.

    Do NOT modify `tsup.config.ts`, `tsconfig.json`, or `scripts/copy-tree-sitter-grammars.mjs` — these are already clean / are red-line protected.
  </action>
  <verify>
    <automated>
      # Deleted paths are gone (file-existence, not grep):
      test ! -e scripts/postinstall.mjs && echo "postinstall.mjs gone" || exit 1
      test ! -e scripts/sync-desktop-version.mjs && echo "sync-desktop-version.mjs gone" || exit 1
      test ! -e .github/workflows/release.yml && echo "release.yml gone" || exit 1

      # package.json: postinstall key absent from scripts AND files array (node assertion, not grep):
      node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); if('postinstall' in p.scripts){console.error('scripts.postinstall still present');process.exit(1);} if((p.files||[]).includes('scripts/postinstall.mjs')){console.error('files array still ships postinstall.mjs');process.exit(1);} console.log('package.json clean');"

      # package.json: critical build/prepare wiring INTACT (positive greps):
      grep -q 'tsup && node scripts/write-cli-package-marker.mjs && node scripts/copy-tree-sitter-grammars.mjs' package.json || { echo "build script tampered"; exit 1; }
      grep -q '"prepare": "simple-git-hooks || true"' package.json || { echo "prepare script missing/changed"; exit 1; }

      # ci.yml: step renamed (positive grep for the NEW name; old name must be gone):
      grep -q 'name: Build (tsup)$' .github/workflows/ci.yml || { echo "ci step not renamed"; exit 1; }
      ! grep -q 'Build (tsup + dashboard)' .github/workflows/ci.yml || { echo "stale step name lingers"; exit 1; }

      # RETAINED workflows still present (red-line / D-02 discretion):
      test -f .github/workflows/release-mirror.yml || { echo "release-mirror.yml must NOT be deleted"; exit 1; }
      test -f .github/workflows/publish-npm.yml || { echo "publish-npm.yml must NOT be deleted"; exit 1; }
      test -f .github/workflows/publish-dsnix.yml || { echo "publish-dsnix.yml must NOT be deleted"; exit 1; }

      # Build chain still green (MVP slice — pure-CLI path usable):
      npm run build || { echo "npm run build FAILED"; exit 1; }
      npm run typecheck || { echo "typecheck FAILED"; exit 1; }

      # Published tarball carries no postinstall artifact:
      npm pack --dry-run 2>&1 | tee /tmp/pack.txt
      ! grep -q 'scripts/postinstall.mjs' /tmp/pack.txt || { echo "tarball still ships postinstall.mjs"; exit 1; }
      grep -q 'dist/package.json' /tmp/pack.txt || true  # CLI ESM marker from write-cli-package-marker.mjs
    </automated>
  </verify>
  <acceptance_criteria>
    - `scripts/postinstall.mjs`, `scripts/sync-desktop-version.mjs`, `.github/workflows/release.yml` do not exist on disk after the task.
    - `package.json` has no `scripts.postinstall` key and the `files` array no longer lists the postinstall script (verified by JSON parse, not grep).
    - `package.json` `scripts.build` still chains `copy-tree-sitter-grammars.mjs`; `scripts.prepare` still reads `simple-git-hooks || true`.
    - `.github/workflows/ci.yml` build step is named exactly `Build (tsup)`; the stale `Build (tsup + dashboard)` label is gone.
    - `release-mirror.yml`, `publish-npm.yml`, `publish-dsnix.yml` remain on disk.
    - `npm run build` and `npm run typecheck` both exit 0.
    - `npm pack --dry-run` output contains no `scripts/postinstall.mjs` entry.
  </acceptance_criteria>
  <done>
    Three dormant desktop build-chain artifacts deleted; `package.json` and `ci.yml` aligned to pure-CLI; build/typecheck exit 0; published tarball carries no desktop installer. The build-chain slice (Success Criterion 1) is clean and the CLI still builds standalone.
  </done>
</task>

<task type="auto">
  <name>Task 2: Trim stale desktop/Rust/Tauri sections from .claude/CLAUDE.md (per D-02)</name>
  <files>.claude/CLAUDE.md</files>
  <read_first>
    - `.claude/CLAUDE.md` — full read. Identify every stale reference to removed surfaces. The stale references cluster in the GSD-managed `## Technology Stack` block (between `<!-- GSD:stack-start -->` and `<!-- GSD:stack-end -->`) and the empty `## Special: Desktop (Tauri) Subdir` heading inside the conventions block. Specific known stale lines (line numbers from the planner read): the Rust language bullet (~L28), the CSS bullet's `dashboard/app.css` / `copy-dashboard-vendor-css.mjs` / `desktop/src/styles.css` refs (~L29), the HTML bullet's `dashboard/index.html` / `desktop/index.html` (~L30), the `Tauri 2 webview runtime` runtime bullet (~L35), the `Browser ... standalone dashboard` bullet (~L36), the npm-workspaces bullet's `desktop/` + `dashboard/` clause (~L38), the `React 19 + Vite 5 — dashboard` framework bullet (~L44), the `Tauri 2 — desktop shell` framework bullet (~L45), the `Vite 5 — dashboard + desktop frontend builds` bullet (~L53), the `@tauri-apps/api` key-dependency bullet (~L71), the Configuration bullets naming `dashboard/tsconfig.json` / `desktop/tsconfig.json` / `dashboard/vite.config.ts` / `desktop/vite.config.ts` / `dashboard/codemirror.js` / `scripts/postinstall.mjs` / `copy-dashboard-vendor-css.mjs` (~L82-87), the vitest bullet's "Tauri mocks" clause (~L85), the Platform Requirements `Rust toolchain` bullet (~L92), the `Desktop: Tauri bundles ...` bullet (~L95), the `Dashboard: standalone static site ... src/server/index.ts` bullet (~L96), the empty `## Special: Desktop (Tauri) Subdir` heading (~L207), the Component Responsibilities `Dashboard server` row (~L235), the Pattern Overview `Multi-surface` bullet's desktop clause (~L248), the Layers entry-locations bullet naming `src/server/`, `src/desktop/` (~L253), and the Entry Points `src/server/index.ts` block (~L306-308).
    - `.planning/phases/04-build-chain-cleanup-full-regression/04-CONTEXT.md` §canonical_refs "Code anchors — 清理目标" — the D-02 trim target list (authoritative).
  </read_first>
  <action>
    Align `.claude/CLAUDE.md` with the pure-CLI reality (D-02). Trim ONLY stale references to surfaces that Phases 1–3 deleted (the Tauri desktop shell, the Rust native layer, the `dashboard/` web frontend, the `src/server/` HTTP dashboard server, `scripts/postinstall.mjs`, `scripts/copy-dashboard-vendor-css.mjs`). Preserve everything still true: the Node ≥22 constraint, the CLI binary entry (`reasonix`/`dsnix` → `dist/cli/index.js`), tsup, the Ink fork, tree-sitter + code-query, Vitest/Biome/Stryker, the i18n 5-locale contract, comment rules, all Conventions, and the whole Constraints/Architecture section that remains accurate (cache-first loop, repair, event-sourced kernel, injectable boundaries).

    Concrete edits (use scoped Edit calls, not a full-file Write):
    1. Languages: delete the Rust language bullet; drop the `dashboard/`/`desktop/` source roots from the TypeScript bullet (leave `src/`, `packages/*/src/`); drop the CSS bullet's dashboard/desktop vendor references (or delete the bullet if nothing true remains); delete the HTML entry-shells bullet (both `dashboard/index.html` and `desktop/index.html` are gone).
    2. Runtime: delete the Tauri 2 webview bullet and the standalone-dashboard browser bullet; trim the npm-workspaces bullet to just `root workspaces: ["packages/*"]` (drop the `desktop/`/`dashboard/` nested-project clause).
    3. Frameworks: delete the `React 19 + Vite 5 — dashboard and desktop` bullet, the `Tauri 2 — desktop shell` bullet, and the `Vite 5 — dashboard + desktop frontend builds` bullet.
    4. Key Dependencies: delete the `@tauri-apps/api` bullet; trim the react-markdown bullet to drop "in dashboard and desktop" (or delete if the libs are no longer used by any live surface — verify against `package.json` deps first; if uncertain, keep the bullet but drop the dashboard/desktop clause).
    5. Configuration: drop `dashboard/tsconfig.json`/`desktop/tsconfig.json` from the tsconfig bullet; drop `dashboard/vite.config.ts`/`desktop/vite.config.ts` from the tsup/bundler bullet; drop `dashboard/codemirror.js` from the biome.json ignores bullet; drop "Tauri mocks" from the vitest bullet; drop `scripts/postinstall.mjs` and `scripts/copy-dashboard-vendor-css.mjs` from the scripts bullet (postinstall is being deleted this phase; copy-dashboard-vendor-css.mjs is already gone).
    6. Platform Requirements: delete the Rust toolchain bullet; delete the `Desktop: Tauri bundles ...` bullet; delete the `Dashboard: standalone static site ...` bullet.
    7. Conventions: delete the empty `## Special: Desktop (Tauri) Subdir` heading.
    8. Architecture (still GSD-managed, stale): delete the `Dashboard server` row from Component Responsibilities; trim the `Multi-surface` pattern bullet to drop "desktop" (keep CLI/TUI + ACP); trim the Layers entry-locations bullet to drop `src/server/` and `src/desktop/`; delete the `src/server/index.ts` Entry Points block.

    If a bullet mixes true and stale content, keep the true part and drop only the stale clause. Do NOT touch the non-GSD sections (Project, Constraints, GSD Workflow Enforcement, Developer Profile) — those are already accurate or are managed by other tooling. If the GSD section markers (`<!-- GSD:stack-start -->` etc.) would be left empty after trimming, leave the markers in place (they are regeneration boundaries, not content).
  </action>
  <verify>
    <automated>
      # Stale surface references are gone (negative greps; allowlisted above):
      [ "$(grep -c 'Tauri' .claude/CLAUDE.md)" = "0" ] || { echo "Tauri refs remain"; grep -n 'Tauri' .claude/CLAUDE.md; exit 1; }
      [ "$(grep -c 'rusqlite' .claude/CLAUDE.md)" = "0" ] || { echo "rusqlite refs remain"; exit 1; }
      [ "$(grep -c 'src/server' .claude/CLAUDE.md)" = "0" ] || { echo "src/server refs remain"; exit 1; }
      [ "$(grep -c 'desktop/src-tauri' .claude/CLAUDE.md)" = "0" ] || { echo "desktop/src-tauri refs remain"; exit 1; }
      [ "$(grep -c 'copy-dashboard-vendor-css' .claude/CLAUDE.md)" = "0" ] || { echo "copy-dashboard-vendor-css refs remain"; exit 1; }

      # Still-true content preserved (positive greps):
      grep -q 'Node ≥22' .claude/CLAUDE.md || { echo "Node constraint lost"; exit 1; }
      grep -q 'copy-tree-sitter-grammars.mjs' .claude/CLAUDE.md || { echo "tree-sitter ref lost"; exit 1; }
      grep -q 'CacheFirstLoop' .claude/CLAUDE.md || { echo "core loop abstraction lost"; exit 1; }
      grep -q 'reasonix' .claude/CLAUDE.md || { echo "CLI binary ref lost"; exit 1; }
      grep -q 'simple-git-hooks' .claude/CLAUDE.md || { echo "git-hook wiring ref lost"; exit 1; }

      # Docs change must not break the build (CLAUDE.md is not compiled, but assert no accidental source edit):
      npm run build || { echo "build regressed after doc edit"; exit 1; }
    </automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of `Tauri`, `rusqlite`, `src/server`, `desktop/src-tauri`, or `copy-dashboard-vendor-css` in `.claude/CLAUDE.md` (the removed surfaces are no longer documented as live).
    - Still-true guidance survives: Node ≥22 constraint, CLI binary entry, tsup, tree-sitter grammars / code-query, the `CacheFirstLoop` core abstraction, the i18n 5-locale contract, comment rules, and the simple-git-hooks wiring all remain documented.
    - The GSD regeneration markers (`<!-- GSD:stack-start -->` … `<!-- GSD:stack-end -->` etc.) are left in place even if a section shrinks.
    - `npm run build` still exits 0 (the doc edit touched no source).
  </acceptance_criteria>
  <done>
    `.claude/CLAUDE.md` describes the pure-CLI project as it actually is — no Rust/Tauri/desktop/dashboard surfaces presented as live. Downstream agents loading this project instruction are no longer misled into searching for removed directories.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build config → CI/publish | `package.json` `files`/`scripts` and `.github/workflows/*.yml` define what ships in the npm tarball and what CI runs — edits here cross into the release supply chain. |
| Project instructions → downstream agents | `.claude/CLAUDE.md` is loaded every session; stale guidance actively misleads future planner/executor/researcher agents. |

## STRIDE Threat Register (ASVS L1 — block on `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Availability (DoS) | `package.json` postinstall/files removal | medium | mitigate | Removing the `scripts.postinstall` key + `files` entry in the same task as deleting the script; `postinstall.mjs` is already a guarded no-op (`existsSync("desktop/package.json")` false → exit 0), and `prepare` (simple-git-hooks) is an independent key. Verify: `npm run build` exits 0, `npm pack --dry-run` lists no postinstall artifact, `prepare` key still present. |
| T-04-02 | Tampering (supply chain) | `.github/workflows/release.yml` deletion | low | accept (risk reduction) | Deleting the dormant Tauri bundling workflow REMOVES the Apple/Windows code-signing + R2/GitHub-updater secret-handling workflow from the repo — a net security improvement. Git history preserves recoverability; CLI npm publish path (`publish-npm.yml`/`publish-dsnix.yml`) and the all-release mirror (`release-mirror.yml`) are retained. |
| T-04-03 | Information disclosure / Integrity | `.claude/CLAUDE.md` trim | low | mitigate | Trim is scoped to stale desktop/Rust/Tauri/dashboard references only; security-relevant constraints (Node ≥22, proxy-install-before-fetch ordering, cache-stability invariant, comment discipline) are explicitly preserved. Verify: positive greps for `Node ≥22`, `CacheFirstLoop`, `simple-git-hooks`, `copy-tree-sitter-grammars.mjs` survive. |
| T-04-SC | Tampering (package legitimacy) | npm package installs | low | accept (N/A) | No `npm install` of new packages occurs this phase — only deletions of dormant scripts and config edits. No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages introduced. Gate not triggered. |

**Block-threshold check:** No threat reaches `high`. The two medium-severity threats (T-04-01 availability, T-04-03 doc integrity) have concrete mitigations embedded in Task 1/Task 2 verify blocks. Proceed.
</threat_model>

<verification>
Phase Success Criterion 1 (PANEL-04) is satisfied when:
- `npm run build` exits 0 with no dashboard/desktop dependency.
- `npm pack --dry-run` ships `dist/`, `data/deepseek-tokenizer.json.gz`, `patches/`, `README.md`, `LICENSE` — and nothing desktop-related.
- `package.json` `scripts.postinstall` and the `files` postinstall entry are gone; `scripts.build` and `scripts.prepare` are intact.
- `.github/workflows/ci.yml` build step is `Build (tsup)`; `release.yml` is deleted; `publish-*.yml` + `release-mirror.yml` retained.
- `.claude/CLAUDE.md` carries zero `Tauri` / `rusqlite` / `src/server` / `desktop/src-tauri` references while preserving Node ≥22, the CLI binary entry, tree-sitter/code-query, and the core loop abstraction.

<human-check>
A maintainer should skim the trimmed `.claude/CLAUDE.md` to confirm the result still reads as coherent project guidance (no dangling bullet fragments, no broken section flow). This is a documentation-quality review, not a functional gate — the automated greps prove the stale refs are gone and the true refs survive.
</human-check>
</verification>

<success_criteria>
- Pure-CLI build chain: `npm run build` green, tarball clean, CI step name accurate, three dormant artifacts deleted, two configs aligned.
- `.claude/CLAUDE.md` aligned to pure-CLI reality; downstream agents no longer misled.
- Red lines honored: `scripts/copy-tree-sitter-grammars.mjs` and `src/code-query/` untouched; `publish-npm.yml`/`publish-dsnix.yml`/`release-mirror.yml` retained; no runtime source code changed.
</success_criteria>

<output>
Create `.planning/phases/04-build-chain-cleanup-full-regression/04-01-SUMMARY.md` when done. Record: files deleted, config diffs, the `release-mirror.yml` retention determination (D-02 discretion), tarball contents before/after, and the build/typecheck exit codes. Hand off to 04-02 (full regression) which depends on this plan's clean build chain.
</output>

## Artifacts this phase produces

This plan produces DELETIONS + CONFIG/DOC EDITS (no new source symbols):

- **Deleted paths:** `scripts/postinstall.mjs`, `scripts/sync-desktop-version.mjs`, `.github/workflows/release.yml`
- **Edited configs:** `package.json` (`scripts.postinstall` key + `files` entry removed), `.github/workflows/ci.yml` (step `Build (tsup + dashboard)` → `Build (tsup)`)
- **Edited docs:** `.claude/CLAUDE.md` (stale desktop/Rust/Tauri/dashboard sections trimmed)

No new modules, types, exports, or runtime code. The only "new" artifacts are the three absences (deleted files) and the aligned config/doc state.
