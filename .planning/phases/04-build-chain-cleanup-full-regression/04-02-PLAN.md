---
phase: 04-build-chain-cleanup-full-regression
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - tests/dashboard-composer-ime.test.tsx
  - tests/dashboard-css-vars.test.ts
  - tests/dashboard-server-bridge-refresh.test.ts
  - tests/dashboard-session-url.test.ts
  - tests/dashboard-settings-baidu.test.tsx
  - tests/dashboard-sidebar-new-chat-layout.test.ts
  - tests/dashboard-token-persistence.test.ts
  - tests/headless-gate-bridges.test.ts
  - tests/headless-host.test.ts
  - tests/mcp-server-list.test.ts
  - tests/version.test.ts
  - .planning/phases/04-build-chain-cleanup-full-regression/04-02-SUMMARY.md
autonomous: true
requirements:
  - SAFE-01
  - SAFE-02
  - SAFE-03
user_setup: []

must_haves:
  truths:
    - "`npm run verify` (build + lint + typecheck + test) exits 0, OR every remaining red is a baseline-proven pre-existing failure recorded with evidence (D-01 zero-regression bar: no NEW red + orphans cleared)"
    - "Every pure-CLI command at least launches its `--help`/startup path without an import-graph break or a stale desktop reference (D-03 hybrid smoke)"
    - "Offline-capable commands (`stats`/`doctor`/`commit`/`sessions`/`replay`/`diff`/`mcp`/`acp`) complete a functional happy-path, not just `--help`"
    - "`scripts/e2e-code-query.mts` and `scripts/e2e-dist-grammars.mts` both pass, proving tree-sitter grammars parse and the dist bundle carries grammar artifacts (D-04)"
    - "The SAFE-02 red lines are intact: `scripts/copy-tree-sitter-grammars.mjs` is still chained in `package.json` `scripts.build`, and `src/code-query/` is present on disk"
  artifacts:
    - path: "tests/dashboard-*.test.ts(x)"
      provides: "(deleted) 7 orphan tests that only covered the removed src/server/ + dashboard UI surfaces"
      expectation: "7 dashboard-* test paths absent from git tree"
    - path: ".planning/phases/04-build-chain-cleanup-full-regression/04-02-SUMMARY.md"
      provides: "regression evidence record — bucket classification, baseline comparison, smoke results, e2e results, acknowledged-deferred items"
      contains: "acknowledged-deferred"
  key_links:
    - from: "package.json scripts.build"
      to: "scripts/copy-tree-sitter-grammars.mjs"
      via: "build script must keep chaining the grammar copy step (SAFE-02 red line verified, not edited)"
      pattern: "copy-tree-sitter-grammars.mjs"
    - from: "scripts/e2e-code-query.mts"
      to: "src/code-query/parser.ts"
      via: "e2e drives the live code-query parser against bundled grammars — proves the symbol-search path end-to-end"
      pattern: "code-query"
    - from: "src/cli/index.ts Commander"
      to: "dist/cli/index.js"
      via: "every command's --help registers via Commander without a desktop import (D-03 import-graph regression net)"
      pattern: "program.command"
---

<!-- planner-discipline-allow: dashboard -->

<objective>
**As a** reasonix-legacy maintainer, **I want to** prove the pure-CLI path is regression-free after the build-chain cleanup, **so that** the M1 milestone can close with confidence: every CLI command still launches, the tree-sitter code-symbol search still works, and `npm run verify` is green (or every remaining red is provably pre-existing and documented).

This plan delivers Success Criteria 2, 3, and 4 of Phase 4 (SAFE-01 command smoke, SAFE-02 tree-sitter/code-query preservation, SAFE-03 full verify). It depends on 04-01's clean build chain. Per the MVP vertical-slice contract, after this plan the pure-CLI release is shippable: build green, commands runnable, symbol search alive, no NEW test regressions.

Purpose: Phases 1–3 left a 22-file pre-existing-red test baseline (STATE.md). D-01 locks a triage-split (delete orphans / fix milestone-caused / evidence-defer truly pre-existing) — NOT a fix-all and NOT silent red. D-03 locks hybrid smoke depth. D-04 locks the e2e-script verification for tree-sitter.
Output: 7 orphan tests deleted, bucket-2 tests fixed green, bucket-3 reds recorded with evidence, smoke + e2e results logged in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-build-chain-cleanup-full-regression/04-CONTEXT.md
@.planning/phases/04-build-chain-cleanup-full-regression/04-01-SUMMARY.md

# Red-line + verification-tool anchors (read before acting):
@scripts/e2e-code-query.mts
@scripts/e2e-dist-grammars.mts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: SAFE-03 test triage — baseline, classify into 3 buckets, act (per D-01)</name>
  <files>tests/dashboard-composer-ime.test.tsx (delete), tests/dashboard-css-vars.test.ts (delete), tests/dashboard-server-bridge-refresh.test.ts (delete), tests/dashboard-session-url.test.ts (delete), tests/dashboard-settings-baidu.test.tsx (delete), tests/dashboard-sidebar-new-chat-layout.test.ts (delete), tests/dashboard-token-persistence.test.ts (delete), tests/headless-gate-bridges.test.ts (possibly fix), tests/headless-host.test.ts (possibly fix), tests/mcp-server-list.test.ts (possibly fix), tests/version.test.ts (possibly fix), .planning/phases/04-build-chain-cleanup-full-regression/04-02-SUMMARY.md</files>
  <read_first>
    - `.planning/STATE.md` §Deferred Items — the 22-file pre-existing-red baseline (dashboard-*, headless-*, version, ssh-remote, mcp-runtime-failures, ui-*, hydrate-cards) and the 03-01 declaration that "全量 vitest 绿是 Phase 4 SAFE-03 职责,无一由 desktop removal 引起".
    - `.planning/phases/04-build-chain-cleanup-full-regression/04-CONTEXT.md` §D-01 (the triage-split standard — 3 buckets + "zero regression = no NEW red + orphans cleared") and §specifics (baseline comparison is the key bucket-3 judgment action).
    - `tests/dashboard-composer-ime.test.tsx`, `tests/dashboard-css-vars.test.ts`, `tests/dashboard-server-bridge-refresh.test.ts`, `tests/dashboard-session-url.test.ts`, `tests/dashboard-settings-baidu.test.tsx`, `tests/dashboard-sidebar-new-chat-layout.test.ts`, `tests/dashboard-token-persistence.test.ts` — confirm each orphan imports ONLY removed surfaces (`src/server/*`, dashboard UI components, `@testing-library` rendering of deleted components). Mirror the Phase 1 convention (01-01 deleted 12 panel-only tests).
    - `tests/headless-gate-bridges.test.ts`, `tests/headless-host.test.ts`, `tests/mcp-server-list.test.ts`, `tests/version.test.ts` — scan to anticipate whether a failure would be milestone-caused (references removed desktop types / src/server) or pre-existing.
    - `.planning/phases/02-bot-decoupling-to-standalone-cli/02-VERIFICATION.md` — the acknowledged-deferred evidence template (D-01 bucket-3 format: filename + failure summary + proof-not-this-phase + leave-for-fix-cycle).
  </read_first>
  <action>
    Execute the D-01 triage-split. Do NOT attempt to fix all 22 historical reds; do NOT silently leave reds either.

    1. **Baseline run.** Run `npm test` and capture the full red-file list (names + failure summaries) to a scratch note. This is the authoritative bucket-input — do not rely on the model-recalled 22-file count (per fabricated-numeric-baselines rule, run the count fresh).

    2. **Bucket 1 — DELETE orphans (no per-file judgment needed).** `git rm` the 7 orphan tests covering only removed `src/server/` + dashboard UI surfaces: `tests/dashboard-composer-ime.test.tsx`, `tests/dashboard-css-vars.test.ts`, `tests/dashboard-server-bridge-refresh.test.ts`, `tests/dashboard-session-url.test.ts`, `tests/dashboard-settings-baidu.test.tsx`, `tests/dashboard-sidebar-new-chat-layout.test.ts`, `tests/dashboard-token-persistence.test.ts`. Before each `git rm`, confirm via the read_first scan that the file imports/renders only deleted modules (if any orphan ALSO covers live core logic, do NOT delete — move it to bucket 2 and strip the desktop-only assertions). This mirrors Phase 1's 01-01 orphan-test deletion convention.

    3. **Bucket 2 — FIX milestone-caused or cheap.** For each of `tests/headless-gate-bridges.test.ts`, `tests/headless-host.test.ts`, `tests/mcp-server-list.test.ts`, `tests/version.test.ts` (and any unexpected red that is NOT in the STATE.md 22-file pre-existing list): determine cause. If the failure is caused by Phases 1–3 (e.g., references a removed `src/server` export, a deleted desktop type, or a stale `0.55.0` version assertion) OR is cheap to repair (one-line stub, mock adjustment, import fix), fix it to green. If git-stash proves it is pre-existing-unrelated (see bucket 3), reclassify.

    4. **Bucket 3 — EVIDENCE-DEFER pre-existing-unrelated.** For `tests/ui-*.test.tsx` (the ~14 TUI component tests), `tests/ssh-remote.test.ts`, `tests/mcp-runtime-failures.test.ts`, `tests/hydrate-cards.test.ts` (STATE.md notes pre-existing Biome-suppression warnings here), and any other red provably predating this milestone: prove pre-existing via baseline comparison — `git stash` this phase's changes (04-01 deletions + this task's orphan deletions), rerun `npm test` for the suspect file, confirm it is STILL red without this phase's changes, then `git stash pop`. Record each deferred file in 04-02-SUMMARY.md using the 02-VERIFICATION acknowledged-deferred format: filename, failure summary, proof-of-pre-existing (the stash result), "leave for fix cycle". Do NOT rabbit-hole on these.

    5. **Zero-regression bar (D-01).** After buckets 1–3, run `npm run verify`. The bar is: build+lint+typecheck green; `npm test` has no NEW red introduced by this phase's build-chain cleanup (04-01 changed only dormant scripts/config/docs — it must not cause test failures); 7 orphans cleared; bucket-2 fixes green; bucket-3 documented. A green verify with documented bucket-3 deferrals satisfies SAFE-03.

    Write the bucket classification, the baseline red-file list, the stash-comparison evidence, and the final `npm run verify` result into `.planning/phases/04-build-chain-cleanup-full-regression/04-02-SUMMARY.md`.
  </action>
  <verify>
    <automated>
      # Bucket 1: 7 orphan tests gone (file-existence, not grep):
      for f in tests/dashboard-composer-ime.test.tsx tests/dashboard-css-vars.test.ts tests/dashboard-server-bridge-refresh.test.ts tests/dashboard-session-url.test.ts tests/dashboard-settings-baidu.test.tsx tests/dashboard-sidebar-new-chat-layout.test.ts tests/dashboard-token-persistence.test.ts; do
        test ! -e "$f" || { echo "orphan still present: $f"; exit 1; }
      done
      echo "7 dashboard orphan tests deleted"

      # SAFE-02 red lines still intact (this task must NOT touch them):
      test -d src/code-query && echo "src/code-query present" || { echo "src/code-query MISSING"; exit 1; }
      test -f scripts/copy-tree-sitter-grammars.mjs || { echo "grammar-copy script MISSING"; exit 1; }
      grep -q 'copy-tree-sitter-grammars.mjs' package.json || { echo "build script no longer chains grammar copy"; exit 1; }

      # SAFE-03 zero-regression bar:
      npm run build || { echo "build FAILED"; exit 1; }
      npm run lint || { echo "lint FAILED"; exit 1; }
      npm run typecheck || { echo "typecheck FAILED"; exit 1; }

      # Final test run — capture result. Bucket-3 deferrals must be documented in SUMMARY,
      # not silently red. Count current red files (fresh count, no hardcoded baseline):
      npm test 2>&1 | tee /tmp/vitest-final.txt
      # If any red remains, require the SUMMARY to acknowledge each:
      if grep -qiE 'failed|FAIL' /tmp/vitest-final.txt; then
        grep -q 'acknowledged-deferred' .planning/phases/04-build-chain-cleanup-full-regression/04-02-SUMMARY.md || { echo "red tests remain but no acknowledged-deferred record in SUMMARY"; exit 1; }
        echo "red tests present but documented as pre-existing-deferred (D-01 bar)"
      else
        echo "vitest fully green"
      fi
    </automated>
  </verify>
  <acceptance_criteria>
    - The 7 named orphan tests no longer exist on disk; each was confirmed (before deletion) to cover only removed `src/server/` or dashboard-UI surfaces.
    - Bucket-2 tests (`headless-*`, `mcp-server-list`, `version`, plus any not in the STATE.md 22-file pre-existing list) are either fixed green OR reclassified to bucket 3 with stash-proven pre-existing evidence.
    - Every bucket-3 deferred red has a record in `04-02-SUMMARY.md`: filename, failure summary, stash-comparison proof of pre-existing, "leave for fix cycle".
    - `npm run build`, `npm run lint`, `npm run typecheck` all exit 0.
    - `npm test` is either fully green OR every remaining red is documented as acknowledged-deferred (D-01 zero-regression bar: no NEW red from this phase's build-chain cleanup, orphans cleared).
    - `src/code-query/` and `scripts/copy-tree-sitter-grammars.mjs` are untouched and the build script still chains the grammar copy.
  </acceptance_criteria>
  <done>
    SAFE-03 triage complete: 7 orphans cleared, milestone-caused reds fixed, pre-existing reds evidence-deferred (not silent, not rabbit-holed), `npm run verify` green-or-documented. The zero-regression bar (D-01) is met.
  </done>
</task>

<task type="auto">
  <name>Task 2: SAFE-01 pure-CLI command smoke — hybrid depth (per D-03)</name>
  <files>.planning/phases/04-build-chain-cleanup-full-regression/04-02-SUMMARY.md</files>
  <read_first>
    - `src/cli/index.ts` — the Commander registration surface; enumerate the live subcommands to smoke. Confirm `reasonix desktop` is the retired stub from 03-01 (prints migration i18n + non-zero exit), NOT a live command.
    - `.planning/phases/04-build-chain-cleanup-full-regression/04-CONTEXT.md` §D-03 — the hybrid depth standard (all-commands --help / offline functional / key-gated live-or-defer).
    - `.planning/phases/01-web-panel-removal/01-02-SUMMARY.md` — the prior `reasonix code` read_file→0.55.0 live-turn smoke pattern (the template for key-gated commands when a key IS present).
    - `.planning/phases/02-bot-decoupling-to-standalone-cli/02-VERIFICATION.md` — the Telegram acknowledged-deferred pattern (the template for key-gated commands when a key is absent).
    - `package.json` `bin` — confirm the binaries (`reasonix`, `dsnix`) resolve to `dist/cli/index.js`.
  </read_first>
  <action>
    Execute the D-03 hybrid smoke. This is the regression net that proves 04-01's build-chain cleanup broke neither the import graph nor any Commander registration. Smoke in three tiers:

    **Tier 1 — ALL commands, `--help`/launch smoke.** For every subcommand registered in `src/cli/index.ts` (`chat`, `code`, `run`, `acp`, `commit`, `sessions`, `replay`, `diff`, `mcp`, `doctor`, `stats`, `update`, `import-sessions`, `prune-sessions`, `version`, `events`, `index`, `mcp-inspect`, `mcp-browse`, `qq`, `telegram`, `weixin`, plus the retired `desktop` stub and bare `--help`): run `node dist/cli/index.js <cmd> --help` (or `tsx src/cli/index.ts <cmd> --help` if dist not current) and confirm exit 0 with a Commander help dump, no import-graph exception, no reference to a deleted module. The retired `desktop` command should print its migration stub and exit non-zero — verify it does NOT attempt to load removed sidecar code. Record pass/fail per command in the SUMMARY.

    **Tier 2 — Offline-capable commands, functional happy-path.** For `stats`, `doctor`, `commit`, `sessions`, `replay`, `diff`, `mcp`, `acp`: drive a real functional path, not just `--help`. Concretely: `reasonix stats` reads real session/usage data and renders; `reasonix doctor` runs its diagnostic checks and exits; `reasonix sessions` lists sessions; `reasonix diff`/`replay` operate on a real or sample transcript; `reasonix mcp` lists/configures MCP; `reasonix acp` starts the NDJSON server and accepts a handshake then exits. Use loopback/fixture inputs where a real session is needed. Tier-2 failures are SAFE-01 regressions — investigate and fix (they would be bucket-2 of D-01, NOT deferred).

    **Tier 3 — Key-gated / TTY / external-service commands.** For `chat`, `code`, `run` (need `DEEPSEEK_API_KEY` + interactive TTY) and `qq`/`telegram`/`weixin` (need bot credentials): if the relevant env var is present, run ONE real turn (mirror 01-02's `reasonix code` read_file→0.55.0 pattern: a single tool round-trip proves the loop). If the key/token is absent, record an acknowledged-deferred entry in the SUMMARY (mirror 02-VERIFICATION Telegram UAT), explicitly noting "key absent, not a regression, deferred to UAT/fix-cycle". For `update`/`import-sessions`/`prune-sessions`: run `--help` plus a dry-run / argument-validation run (no destructive mutation).

    Record all three tiers' results in `04-02-SUMMARY.md`. Any Tier-1/Tier-2 failure is a blocking regression (fix before this plan completes); Tier-3 key-absent is acknowledged-deferred (non-blocking).
  </action>
  <verify>
    <automated>
      # Tier 1: every command launches --help (import-graph + Commander regression net).
      # Build first so dist is current:
      npm run build || { echo "build FAILED before smoke"; exit 1; }
      CMD_LIST="chat code run acp commit sessions replay diff mcp doctor stats update import-sessions prune-sessions version events"
      fail=0
      for c in $CMD_LIST; do
        if node dist/cli/index.js "$c" --help >/dev/null 2>&1; then
          echo "OK  --help: $c"
        else
          ec=$?
          # Some commands (e.g. bare chat/code) may exit non-zero on --help under TTY guards;
          # treat exit codes 0 and the "help shown" case as pass. Fail on import/registration exceptions.
          node dist/cli/index.js "$c" --help 2>&1 | grep -qiE 'cannot find module|Error: Cannot|is not defined' && { echo "IMPORT BREAK: $c"; fail=1; } || echo "OK (non-zero help) : $c"
        fi
      done
      # Retired desktop stub: must NOT load sidecar code; prints migration text:
      node dist/cli/index.js desktop --help 2>&1 | tee /tmp/desktop-stub.txt
      ! grep -qiE 'cannot find module|src/desktop|sidecar' /tmp/desktop-stub.txt || { echo "desktop stub references removed sidecar"; fail=1; }
      [ "$fail" = "0" ] || exit 1

      # Tier 2 spot-check (offline functional): stats + doctor must exit 0 on real data:
      node dist/cli/index.js stats >/dev/null 2>&1 || { echo "stats functional smoke FAILED (Tier-2 regression)"; exit 1; }
      node dist/cli/index.js doctor >/dev/null 2>&1 || { echo "doctor functional smoke FAILED (Tier-2 regression)"; exit 1; }
      echo "Tier-2 stats/doctor functional smoke OK"

      # Tier 3: key-gated commands — record outcome without blocking if key absent:
      if [ -n "$DEEPSEEK_API_KEY" ]; then echo "DEEPSEEK_API_KEY present — Tier-3 live turn should be attempted"; else echo "DEEPSEEK_API_KEY absent — Tier-3 chat/code/run acknowledged-deferred (non-blocking)"; fi
      grep -q 'SAFE-01 smoke' .planning/phases/04-build-chain-cleanup-full-regression/04-02-SUMMARY.md || { echo "smoke results not recorded in SUMMARY"; exit 1; }
    </automated>
  </verify>
  <acceptance_criteria>
    - Tier 1: every registered subcommand's `--help` exits without an import-graph exception or a deleted-module reference; the retired `desktop` stub prints its migration message and does NOT load removed sidecar code.
    - Tier 2: `stats`, `doctor`, and the other offline-capable commands complete a real functional path (exit 0 on real data) — no functional regression.
    - Tier 3: key-gated commands either complete one real turn (key present) OR are recorded as acknowledged-deferred (key absent) in `04-02-SUMMARY.md`.
    - All three tiers' results are written to `04-02-SUMMARY.md`.
  </acceptance_criteria>
  <done>
    Every pure-CLI command proven to launch; offline commands proven functional; key-gated commands either exercised live or explicitly deferred. SAFE-01 (D-03 hybrid smoke) satisfied with documented evidence.
  </done>
</task>

<task type="auto">
  <name>Task 3: SAFE-02 tree-sitter / code-query preservation verify (per D-04)</name>
  <files>.planning/phases/04-build-chain-cleanup-full-regression/04-02-SUMMARY.md</files>
  <read_first>
    - `scripts/e2e-code-query.mts` — the purpose-built e2e for the code-query parser; confirm how it is meant to run (likely `npx tsx scripts/e2e-code-query.mts`) and what it asserts (grammar parse + symbol extraction).
    - `scripts/e2e-dist-grammars.mts` — the purpose-built e2e that the dist bundle carries the grammar artifacts; confirm the runner and the assertion.
    - `package.json` `scripts.build` — confirm `copy-tree-sitter-grammars.mjs` is still chained (Task 1 already verified, re-confirm here as the SAFE-02 red line).
    - `.planning/phases/04-build-chain-cleanup-full-regression/04-CONTEXT.md` §D-04 — the verification standard (e2e scripts are preferred; escalate to a live `reasonix code` `code-query` turn ONLY if the e2e scripts are themselves red/missing).
  </read_first>
  <action>
    Execute the D-04 SAFE-02 verification. The bar is: the two existing purpose-built e2e scripts pass, proving the tree-sitter grammars still parse and the dist bundle still ships grammar artifacts. This is stronger than a static "the build step is still listed" check and lighter than a live agent turn.

    1. **Red-line presence check.** Confirm `scripts/copy-tree-sitter-grammars.mjs` exists on disk, `src/code-query/` is a non-empty directory, and `package.json` `scripts.build` still chains the grammar-copy step. These are the SAFE-02 red lines — they must NOT have been touched by 04-01 or by Task 1 of this plan.

    2. **Run `scripts/e2e-dist-grammars.mts`.** This proves the dist bundle (produced by 04-01's still-green `npm run build`) carries the grammar artifacts. Run it with the same runner the script header documents (default: `npx tsx scripts/e2e-dist-grammars.mts`). If it passes, record the result.

    3. **Run `scripts/e2e-code-query.mts`.** This drives the live code-query parser (`src/code-query/parser.ts`) against the bundled grammars — end-to-end proof that symbol extraction works. Run with `npx tsx scripts/e2e-code-query.mts` (or the documented runner). If it passes, record the result.

    4. **Escalation (D-04, only if e2e is broken).** If either e2e script is red or missing, escalate: first attempt to repair the e2e script (it may be a stale fixture); if repair is non-trivial, run ONE live `reasonix code` turn that invokes the `code-query` tool (mirror 01-02's read_file→version pattern, but targeting a symbol query) to prove the runtime path. If the live turn ALSO fails, that is a blocking SAFE-02 regression — do NOT defer. Record which path was taken in the SUMMARY.

    5. **Record.** Write the e2e results (and any escalation) into `04-02-SUMMARY.md` under a SAFE-02 heading. The goal is evidence that tree-sitter grammars + code-query survived the build-chain cleanup intact.
  </action>
  <verify>
    <automated>
      # Red-line presence:
      test -f scripts/copy-tree-sitter-grammars.mjs || { echo "grammar-copy script MISSING (SAFE-02 red line)"; exit 1; }
      test -d src/code-query || { echo "src/code-query MISSING (SAFE-02 red line)"; exit 1; }
      ls src/code-query/*.ts >/dev/null 2>&1 || { echo "src/code-query/ empty"; exit 1; }
      grep -q 'copy-tree-sitter-grammars.mjs' package.json || { echo "build script no longer chains grammar copy"; exit 1; }

      # Fresh build so dist is current for the dist-grammars e2e:
      npm run build || { echo "build FAILED before e2e"; exit 1; }

      # D-04 primary verification: run both purpose-built e2e scripts.
      # Use tsx (the repo's documented script runner); if a script documents a different runner, prefer that.
      npx tsx scripts/e2e-dist-grammars.mts || { echo "e2e-dist-grammars FAILED — escalate per D-04"; exit 1; }
      npx tsx scripts/e2e-code-query.mts || { echo "e2e-code-query FAILED — escalate per D-04"; exit 1; }
      echo "SAFE-02 e2e both green"

      # Evidence recorded:
      grep -q 'SAFE-02' .planning/phases/04-build-chain-cleanup-full-regression/04-02-SUMMARY.md || { echo "SAFE-02 results not in SUMMARY"; exit 1; }
    </automated>
  </verify>
  <acceptance_criteria>
    - `scripts/copy-tree-sitter-grammars.mjs`, `src/code-query/` (non-empty), and the `copy-tree-sitter-grammars.mjs` chaining in `package.json` `scripts.build` are all intact (SAFE-02 red lines honored).
    - `scripts/e2e-dist-grammars.mts` passes (dist carries grammar artifacts).
    - `scripts/e2e-code-query.mts` passes (live parser extracts symbols via bundled grammars).
    - If either e2e failed and was escalated per D-04, the escalation path and outcome are recorded; a live `reasonix code` code-query turn was used only if e2e repair was impractical.
    - Results recorded under a SAFE-02 heading in `04-02-SUMMARY.md`.
  </acceptance_criteria>
  <done>
    SAFE-02 (D-04) satisfied: tree-sitter grammars build into dist and the code-query parser extracts symbols end-to-end. The code-symbol-search capability — a CLI feature that must survive M1 — is proven alive after the build-chain cleanup.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite → regression signal | `npm run verify` is the pre-push gate; a silently-red or mis-triaged suite would let real regressions through. |
| CLI surface → import graph | Every `reasonix <cmd> --help` exercises Commander registration + the import graph; a broken import is a user-visible regression. |
| Key-gated runtime → external API | Tier-3 live turns (chat/code/run) send real requests to DeepSeek using `DEEPSEEK_API_KEY`; logging must not capture secrets. |

## STRIDE Threat Register (ASVS L1 — block on `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Availability (coverage loss) | orphan-test deletion (bucket 1) | medium | mitigate | Only delete tests confirmed (via read_first scan) to cover REMOVED surfaces (`src/server/`, dashboard UI). If an orphan ALSO covers live core logic, reclassify to bucket 2 and strip the desktop-only assertions rather than deleting. Phase 1's 01-01 precedent validates the convention. |
| T-04-05 | Integrity (false baseline) | bucket-3 evidence-defer classification | medium | mitigate | Every "pre-existing" claim is proven by `git stash` baseline comparison (stash this phase's changes, rerun the suspect test, confirm still red) before deferring. The classification is recorded per-file in 04-02-SUMMARY.md for audit. A real regression cannot hide behind a "pre-existing" label. |
| T-04-06 | Information disclosure (secret logging) | Tier-3 live turns with DEEPSEEK_API_KEY | low | accept | The smoke reuses the existing `DeepSeekClient` (no new logging code introduced this phase); `src/telemetry/usage.ts` writes only usage aggregates to JSONL, not request bodies or keys. No new secret surface added. |
| T-04-07 | Tampering (false green) | verify gate that swallows red | low | mitigate | The Task-1 verify block requires any remaining red to be matched by an `acknowledged-deferred` record in the SUMMARY — it cannot silently pass on red. The `tee /tmp/vitest-final.txt` + grep pattern surfaces failures. |

**Block-threshold check:** No threat reaches `high`. The two medium threats (T-04-04 coverage, T-04-05 false baseline) have concrete mitigations embedded in the task actions and verify blocks. Proceed.
</threat_model>

<verification>
Phase Success Criteria 2, 3, 4 (SAFE-01/02/03) are satisfied when:
- **SAFE-01:** Tier-1 all-command `--help` clean (no import breaks); Tier-2 offline functional smoke green; Tier-3 key-gated commands live-or-documented-deferred.
- **SAFE-02:** Both e2e scripts green; red lines (`copy-tree-sitter-grammars.mjs`, `src/code-query/`) intact.
- **SAFE-03:** `npm run verify` green OR every remaining red is acknowledged-deferred with stash-proven pre-existing evidence (D-01 zero-regression bar).

<human-check>
After execution, a maintainer (or the `/gsd-verify-phase` UAT flow) should:
1. Spot-check the 04-02-SUMMARY bucket classifications — confirm no bucket-2 file was quietly deferred.
2. If a Tier-3 live turn ran (DeepSeek key present), confirm the single turn completed a tool round-trip without error.
3. Confirm the milestone can close: pure-CLI build green, commands runnable, symbol search alive, no NEW regressions.
</human-check>
</verification>

<success_criteria>
- SAFE-03 zero-regression bar met: no NEW red from the build-chain cleanup, 7 orphans cleared, bucket-2 fixed, bucket-3 evidence-deferred.
- SAFE-01 hybrid smoke complete across all three tiers with results recorded.
- SAFE-02 e2e green; tree-sitter grammars + code-query proven alive; red lines honored.
- `npm run verify` green or documented-deferred — M1 milestone closeable.
</success_criteria>

<output>
Create `.planning/phases/04-build-chain-cleanup-full-regression/04-02-SUMMARY.md` when done. Structure it with headings for: SAFE-03 triage (baseline red list, bucket-1 deletions, bucket-2 fixes, bucket-3 acknowledged-deferred with stash evidence, final `npm run verify` result), SAFE-01 smoke (Tier-1/2/3 results per command), SAFE-02 e2e (both scripts + red-line check + any escalation). This SUMMARY is the evidence artifact for the `/gsd-verify-phase` UAT and the M1 milestone close.
</output>

## Artifacts this phase produces

This plan produces TEST DELETIONS + an EVIDENCE RECORD (no new source symbols):

- **Deleted tests (bucket 1):** 7 `tests/dashboard-*.test.ts(x)` orphans covering only removed `src/server/` + dashboard UI surfaces.
- **Fixed tests (bucket 2, if applicable):** `tests/headless-*.test.ts`, `tests/mcp-server-list.test.ts`, `tests/version.test.ts` — milestone-caused or cheap repairs, driven green.
- **Evidence record:** `.planning/phases/04-build-chain-cleanup-full-regression/04-02-SUMMARY.md` — bucket classifications, baseline/stash comparison proof, SAFE-01 smoke results (Tier 1/2/3), SAFE-02 e2e results, acknowledged-deferred items (bucket 3 + key-absent Tier-3 commands).

No new modules, types, exports, or runtime code. The deliverable is the absence of orphan tests, the green/documented verify state, and the evidence SUMMARY.
