---
phase: 11-supply-chain-and-security-contract
plan: 01
type: execute
wave: 1
autonomous: true
requirements: [SEC-01, SEC-02, SEC-04]
files_modified:
  - package.json
  - package-lock.json
  - docs/install-script-provenance.md
  - docs/README.md
  - scripts/check-docs.mjs
  - tests/install-script-provenance.test.ts
must_haves:
  truths:
    - "The direct production dependencies undici and ws are updated to compatible secure 8.x versions and package-lock.json records the reviewed resolution."
    - "A fresh npm audit --omit=dev reports no high or critical production vulnerabilities."
    - "Repository install-script provenance is documented and automated verification fails if a new production install script appears without review."
  artifacts:
    - package.json
    - package-lock.json
    - docs/install-script-provenance.md
    - tests/install-script-provenance.test.ts
  key_links:
    - "package.json dependency ranges and package-lock resolutions must agree on the remediated undici/ws versions."
    - "docs/install-script-provenance.md must match the current package-lock hasInstallScript set and the root prepare script."
---

<objective>
Remove the current production audit blockers and turn npm install-script review into a maintained, test-backed contract. Phase 11 must not merely note the risk — it must leave the repository with a clean production audit and an auditable provenance record for every install script still present.
</objective>

<context>
@.planning/phases/11-supply-chain-and-security-contract/11-CONTEXT.md
@.planning/REQUIREMENTS.md
@package.json
@package-lock.json
@.planning/quick/260717-bvq-assess-stable-release/260717-bvq-SUMMARY.md
@scripts/check-docs.mjs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Remediate direct production dependency findings</name>
  <files>package.json, package-lock.json</files>
  <action>Update the direct production dependencies `undici` and `ws` to secure compatible `8.x` versions, regenerate `package-lock.json` on the pinned Windows/npm baseline, and keep the rest of the maintained package surface stable. Do not hide findings with audit suppressions, legacy compatibility shims, or Phase-12 workflow changes.</action>
  <verify><automated>npm audit --omit=dev --json</automated></verify>
  <done>The production dependency graph is reproducible from package-lock.json and contains no high/critical audit findings.</done>
</task>

<task type="auto">
  <name>Task 2: Record and guard npm install-script provenance</name>
  <files>docs/install-script-provenance.md, docs/README.md, scripts/check-docs.mjs, tests/install-script-provenance.test.ts</files>
  <action>Create a maintained provenance document that records the root `prepare` script and every current `package-lock.json` `hasInstallScript` entry, including why it runs and whether it is dev-only or optional. Link it from the docs hub. Add an automated guard that fails if the provenance doc is missing, if its reviewed package set drifts from package-lock.json, or if a non-reviewed production install script appears.</action>
  <verify><automated>node scripts/check-docs.mjs &amp;&amp; npm test -- --run tests/install-script-provenance.test.ts</automated></verify>
  <done>Maintainers can audit every install script from tracked repository evidence, and future drift fails verification.</done>
</task>
</tasks>

<verification>
- `npm audit --omit=dev --json` shows 0 high and 0 critical production vulnerabilities.
- `node scripts/check-docs.mjs` passes with the new install-script provenance doc linked from `docs/README.md`.
- Focused provenance tests prove the reviewed install-script set matches package-lock.json and that no non-optional production dependency currently requires install scripts.
</verification>

<success_criteria>
- SEC-01, SEC-02, and SEC-04 are directly observable from repository artifacts and local verification.
- No install-script approval is implicit or undocumented.
- The phase does not borrow CI/publication work from later phases.
</success_criteria>

<output>Create `.planning/phases/11-supply-chain-and-security-contract/11-01-SUMMARY.md` when done.</output>
