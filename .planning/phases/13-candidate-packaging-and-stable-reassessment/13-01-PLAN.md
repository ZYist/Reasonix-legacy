---
phase: 13-candidate-packaging-and-stable-reassessment
plan: 01
type: execute
wave: 1
autonomous: true
requirements: [PKG-01, PKG-02, PKG-03]
files_modified:
  - .planning/phases/13-candidate-packaging-and-stable-reassessment/13-01-SUMMARY.md
must_haves:
  truths:
    - "A clean `npm ci` followed by `npm run verify` passes on Windows with Node.js 24.15.0 and npm 11.16.0."
    - "`npm pack --dry-run --json` proves the candidate tarball includes the maintained runtime surface and excludes `.planning`, removed surfaces, and `dsnix` artifacts."
    - "Installing the real tarball into a fresh temporary directory produces only the `reasonix-legacy` executable and the packaged CLI responds to `--version` and `--help`."
  artifacts:
    - .planning/phases/13-candidate-packaging-and-stable-reassessment/13-01-SUMMARY.md
  key_links:
    - "The tarball contract must preserve the identity rules established in Phase 10 and the npm-only release contract established in Phase 12."
    - "Isolated install evidence must come from a fresh temp directory, not from the developer checkout's existing bins."
---

<objective>
Generate concrete local evidence that the current tree can become a v1.3 stable candidate: clean verify, pack inventory, and isolated tarball install checks.
</objective>

<context>
@.planning/phases/13-candidate-packaging-and-stable-reassessment/13-CONTEXT.md
@.planning/REQUIREMENTS.md
@package.json
@package-lock.json
@.planning/phases/10-package-identity-unification/10-VERIFICATION.md
@.planning/phases/12-windows-release-automation/12-02-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Produce clean verification evidence</name>
  <action>Run `npm ci` and then `npm run verify` on the maintained Windows/Node/npm baseline. Capture command outcomes and keep the full test suite intact.</action>
  <verify><automated>node --version &amp;&amp; npm --version &amp;&amp; npm ci &amp;&amp; npm run verify</automated></verify>
  <done>There is fresh local evidence that the candidate passes the full release verification baseline from a clean dependency state.</done>
</task>

<task type="auto">
  <name>Task 2: Capture tarball and isolated install evidence</name>
  <action>Use `npm pack --dry-run --json` to inspect the candidate contents, then create the real tarball and install it into a fresh temporary directory. Verify that `reasonix-legacy --version` and `reasonix-legacy --help` work and that `reasonix` / `dsnix` executables are absent.</action>
  <verify><automated>npm pack --dry-run --json &amp;&amp; npm pack</automated></verify>
  <done>The candidate tarball contents and isolated install behavior are recorded with exact local evidence.</done>
</task>
</tasks>

<verification>
- Fresh `npm ci` + `npm run verify` passes on Windows/Node.js `24.15.0`/npm `11.16.0`.
- Pack dry-run evidence shows the required runtime files and excludes forbidden surfaces.
- A fresh install of the tarball exposes only `reasonix-legacy` and the packaged CLI responds to `--version` and `--help`.
</verification>

<success_criteria>
- PKG-01, PKG-02, and PKG-03 are directly evidenced by local commands.
- No fake publish or remote action is performed.
- Candidate evidence is specific enough to quote directly in the final reassessment report.
</success_criteria>

<output>Create `.planning/phases/13-candidate-packaging-and-stable-reassessment/13-01-SUMMARY.md` when done.</output>
