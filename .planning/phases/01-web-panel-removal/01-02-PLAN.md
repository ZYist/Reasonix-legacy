---
phase: 01-web-panel-removal
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/i18n/types.ts
  - src/i18n/EN.ts
  - src/i18n/zh-CN.ts
  - src/i18n/JA.ts
  - src/i18n/de.ts
  - src/i18n/ru.ts
autonomous: false
requirements: [PANEL-03]
tags: [removal, i18n, runtime-smoke, cli]

must_haves:
  truths:
    - "`src/i18n/types.ts` 与 5 个 locale 文件(EN/zh-CN/JA/de/ru)不再含面板专用 i18n 键,且键集一致(tsc 通过)"
    - "`reasonix chat` TUI 能正常发起并完成一轮对话(发消息 → 收模型回复 → 执行一次工具),无面板相关报错"
    - "`reasonix code --help` 不再列出 `--no-dashboard`/`--dashboard-port`/`--dashboard-host` 选项"
    - "`npm run typecheck` 与 `npm run build` 在 01-01 绿的基础上继续绿"
  artifacts:
    - "`src/i18n/types.ts` 与 5 个 locale 文件的面板专用键全部移除"
    - "面板残留基线快照(剩余 dashboard 字面量仅限被允许的叙事注释,记录于 SUMMARY)"
  key_links:
    - "TUI 启动 → 消息发送 → 模型流式回复 → 工具派发与结果回显 的运行时链路无面板代码介入,无 ESM 动态 import 失败"
    - "i18n 类型契约(types.ts)与 locale 实现(5 文件)键集对齐,无悬挂键"
---

## Phase Goal

**As a** CLI 用户,**I want to** 在不依赖任何 Web 面板代码的前提下运行 `reasonix chat`/`code`/`run`,**so that** 终端里的 DeepSeek 编程 agent 不再背负面板的耦合与构建负担。

本计划(01-02)交付第二个垂直切片:清理 01-01 之后剩余的 CLI 面板适配残骸(主要是 i18n 死串),并通过运行时冒烟验证「`reasonix chat` TUI 完成一轮对话」这一 ROADMAP 成功标准。完成后 Phase 1 收尾,核心零回归红线得到运行时证据。

<objective>
清理剩余面板 i18n 残骸并完成运行时 TUI 冒烟验证。

Purpose: 01-01 切断了所有会破坏构建的面板耦合,但 i18n 文件里仍残留大量面板专用键(端口提示、token 重置、状态行 URL、/dashboard handler 文案等)—— 它们不破坏构建,却是 PANEL-03「CLI 内部面板适配代码清除完毕」要求清理的。同时,01-01 的验证是静态的(typecheck/build/lint);ROADMAP 成功标准 3 要求运行时证据(TUI 一轮对话),需要交互式 TTY 与 DeepSeek key,只能由人确认。
Output: 6 个 i18n 文件清理;TUI 运行时冒烟通过的人确认记录。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/01-web-panel-removal/01-01-PLAN.md

# 01-01 交付后的事实基础
# - src/ 下已无任何对 src/server/ 的 import,broadcastDashboardEvent/startDashboard/saveDashboardPort/--no-dashboard 等标识全 src/ 为 0。
# - 剩余的 dashboard 字面量主要集中在 src/i18n/(types.ts 契约 + EN/zh-CN/JA/de/ru 五个 locale 实现)。
# - 已知 i18n 面板键(来自 01-01 grep 基线):
#     ui.dashboardPortHint、ui.dashboardPortInvalid、ui.noDashboard、ui.dashboardAutoStartFailed
#     handlers.dashboard.{notAvailable, stopNoCallback, notRunning, stopping, copied, tokenReset, tokenResetting, ready, readyHint, failed, alreadyRunning, alreadyRunningHint}
#     statusDash
#   注:具体键名以实际 grep 为准,执行时先 grep -n "dashboard" src/i18n/types.ts 取完整清单,再同步改 5 个 locale。
# - src/cli/ui/markdown.tsx 顶部注释提到「mirrors dashboard/app.css .md rules」—— 属叙事性注释,顺手清理(不破坏构建)。
# - config.ts 注释里「/model <id> and the dashboard model picker」在 01-01 已顺手改;若残留此处一并清理。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 清理 i18n 面板专用死串 + 收敛基线快照</name>
  <files>
    src/i18n/types.ts
    src/i18n/EN.ts
    src/i18n/zh-CN.ts
    src/i18n/JA.ts
    src/i18n/de.ts
    src/i18n/ru.ts
  </files>
  <read_first>
    - src/i18n/types.ts( 完整读;定位所有 dashboard 相关键:ui.dashboardPortHint/ui.dashboardPortInvalid/ui.noDashboard/ui.dashboardAutoStartFailed、handlers.dashboard.* 段、statusDash 等 )
    - src/i18n/EN.ts( 对照确认每个面板键的英文实现;EN 是契约的事实来源 )
    - src/i18n/zh-CN.ts( 确认中文实现 )
    - src/i18n/JA.ts( 确认日文实现 )
    - src/i18n/de.ts( 确认德文实现;行 153-159、1102-1110、1168 为已知面板键位置 )
    - src/i18n/ru.ts( 确认俄文实现;行 144-150 为已知面板键位置 )
    - src/cli/ui/markdown.tsx:1( 顶部叙事注释,顺手清理 )
    - 01-01 的 SUMMARY(若已生成,确认其基线 grep 计数)
  </read_first>
  <behavior>
    - src/i18n/types.ts 中所有面板专用键(ui.dashboardPortHint、ui.dashboardPortInvalid、ui.noDashboard、ui.dashboardAutoStartFailed、handlers.dashboard 整段、statusDash 等)被移除
    - 5 个 locale 文件(EN/zh-CN/JA/de/ru)同步移除对应键值,键集与 types.ts 完全一致
    - i18n 类型契约一致性由 tsc 验证:多删 locale 键会触发「Missing keys」类型错;多留 locale 键会触发「Excess property」错
    - src/cli/ui/markdown.tsx 顶部关于面板 CSS 的叙事注释被改写为不引用面板的中性描述(或删除整段注释,保留代码逻辑)
    - 收敛 grep 基线:记录清理后 src/ 下剩余 dashboard 字面量计数(预期仅剩允许的叙事性注释),写入 SUMMARY 供后续 phase drift 检查参考
  </behavior>
  <action>
    1. 先跑 grep -n "dashboard" src/i18n/types.ts 取本机当前完整面板键清单(键名以实际为准,不要假设);同步对 5 个 locale 文件跑相同 grep 取行号。
    2. 在 types.ts 中删除所有面板专用键声明:涵盖端口提示/端口非法/不启动面板/自启失败等 UI 提示键、整个 handlers.dashboard 子段(含 notAvailable/stopNoCallback/notRunning/stopping/copied/tokenReset/tokenResetting/ready/readyHint/failed/alreadyRunning/alreadyRunningHint 等子键)、statusDash 状态行键。删除时连带删除上方文档注释。
    3. 在 EN.ts、zh-CN.ts、JA.ts、de.ts、ru.ts 五个 locale 文件中,按 types.ts 删除的键同步删除对应值字符串。EN.ts 是契约事实来源,先改 EN 再对齐其他四种语言;每个 locale 的键集必须与 types.ts 完全一致(多删少留都会被 tsc 捕获)。
    4. 顺手清理 src/cli/ui/markdown.tsx 顶部关于「mirrors dashboard/app.css」的叙事注释,改写为不引用已删除面板的中性描述(例如「visual mapping for markdown rendering」),保留 marked 解析与 cli-highlight ANSI 着色逻辑。
    5. 顺手扫描 src/ 下是否还有遗漏的面板叙事性注释(非 i18n 文件内),例如 config.ts 残留的「dashboard model picker」、App.tsx 残留的考古注释 —— 若发现且属于违反 CLAUDE.md「无版本/事故叙事」的,一并清理;不违反规则的隐藏约束注释保留。
    6. 运行 npm run lint:fix 让 Biome 整理格式,再跑 npm run typecheck(验证 i18n 契约一致)、npm run build、npm run lint。
    7. 跑 grep -rn "dashboard" src/ | wc -l 记录剩余计数,写入 SUMMARY 的「收敛基线」段;剩余字面量预期仅限:被允许的叙事性注释(如历史变更说明,若不违反规则)、本计划保留的 grep 测试字面量等。该计数将作为 Phase 4 drift 检查的基线。
  </action>
  <verify>
    <automated>npm run typecheck && npm run build && npm run lint && echo "i18n-clean"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` 退出码 0(i18n 契约一致,无 missing/excess keys)
    - `npm run build` 退出码 0
    - `npm run lint` 退出码 0
    - src/i18n/types.ts 不再含面板专用键:`grep -c "dashboardPortHint\|dashboardPortInvalid\|noDashboard\|dashboardAutoStartFailed\|handlers\.dashboard\|statusDash" src/i18n/types.ts` 返回 0
    - 5 个 locale 文件同步:`for f in EN zh-CN JA de ru; do grep -c "dashboardPortHint\|noDashboard\|statusDash" src/i18n/$f.ts; done` 每个返回 0
    - 收敛基线快照:`grep -rn "dashboard" src/ | wc -l` 的计数已记录进 SUMMARY(不强求为 0,允许保留被允许的叙事注释)
  </acceptance_criteria>
  <done>i18n 契约与 5 个 locale 实现键集对齐,面板专用死串全部移除,typecheck/build/lint 三件套在 01-01 基础上继续绿;收敛基线快照已记录供后续 phase 参考。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 运行时冒烟 —— reasonix chat TUI 完成一轮对话 + CLI 选项核对</name>
  <files>无新文件改动(本任务为运行时人交互验证,不修改源码)</files>
  <what-built>
    01-01 切除了 Web 面板表面与所有硬耦合,01-02 清理了 i18n 死串。静态验证(typecheck/build/lint)已通过,但 ROADMAP 成功标准 3 要求运行时证据:「reasonix chat TUI 能正常发起并完成一轮对话(发消息 → 收模型回复 → 执行一次工具),无面板相关报错」。此验证需要交互式 TTY 与有效的 DeepSeek API key,只能由人确认。同时核对 CLI 选项面板标志已下线。
  </what-built>
  <verify>
    <human-check>人交互确认:`reasonix code --help` 无面板选项 + `reasonix chat` TUI 完成一轮对话(消息→模型回复→工具执行回显)+ /dashboard 安全拒绝 + 退出无句柄泄漏。详见下方 how-to-verify。</human-check>
  </verify>
  <done>`reasonix code --help` 输出无 --no-dashboard 系列选项;`reasonix chat` TUI 启动无动态 import 失败、完成一轮带工具调用的对话;输入 /dashboard 不引发崩溃;退出无端口/句柄泄漏报错。</done>
  <read_first>
    - ~/.reasonix/config.json 或环境变量 DEEPSEEK_API_KEY( 确认有可用 key;若无,先 reasonix setup )
    - src/cli/index.ts( 确认 chat 命令注册与默认启动路径 )
    - src/cli/commands/chat.tsx( 确认 TUI 入口,无面板依赖 )
    - 01-01 与 01-02 的 acceptance grep 结果( 确认静态层已绿 )
  </read_first>
  <behavior>
    - `reasonix code --help`(或 `npm run dev -- code --help`)输出不再含 --no-dashboard、--dashboard-port、--dashboard-host 三个选项
    - `reasonix chat`(或 `npm run chat`)TUI 能成功启动,渲染输入框,无 ESM 动态 import 解析失败、无 ReferenceError、无面板相关报错
    - 在 TUI 内输入一条简单编程请求(例如「列出当前目录文件」),模型流式回复正常显示,且至少触发一次工具调用(如 run_command/filesystem)并回显结果,完成一轮 turn
    - 在 TUI 内输入 /dashboard(或 /help 查看是否还列该命令),确认该命令已被移除(派发时返回未知命令或不在帮助列表),不引发非空断言崩溃
    - 退出 TUI(Esc 或 /exit)正常,无遗留监听端口、无未关闭的句柄报错
  </behavior>
  <action>
    执行者(Claude)在抵达此 checkpoint 前已完成的工作:01-01 与 01-02 Task 1 的全部静态改造与 grep/typecheck/build/lint 验证。本任务由人接管交互式验证。
  </action>
  <how-to-verify>
    1. CLI 选项核对(执行者可代跑,作为预检):
       - 跑 `node dist/cli/index.js code --help`(若已 build)或 `npm run dev -- code --help`
       - 确认输出不含 --no-dashboard、--dashboard-port、--dashboard-host
       - 跑 `node dist/cli/index.js --help` 确认顶层命令列表正常
    2. TUI 一轮对话(人交互,需 TTY + DeepSeek key):
       - 在终端跑 `npm run chat`(或 `node dist/cli/index.js chat`)
       - 确认 TUI 启动无报错(特别留意有无「Cannot find module '../../server/index.js'」之类的动态 import 失败)
       - 输入一条会触发工具的简单请求,例如:列出当前目录的文件 或 读一下 package.json 的 version 字段
       - 观察:模型流式回复正常渲染、至少一次工具调用卡片显示并回显结果、turn 正常结束
       - 输入 /dashboard 测试残留命令是否被安全拒绝(预期:未知命令提示,而非崩溃)
       - 输入 /exit 退出,观察无端口/句柄泄漏报错
    3. 异常处理:若 TUI 启动失败或工具调用崩溃,记录错误堆栈,回退到 01-01/01-02 Task 1 修复后重验;不要跳过。
  </how-to-verify>
  <resume-signal>验证通过请回复「approved」并附 TUI 一轮对话的关键截图或文本摘要;若有报错,请粘贴错误堆栈或描述异常行为。</resume-signal>
  <acceptance_criteria>
    - `node dist/cli/index.js code --help`(或 `npm run dev -- code --help`)输出不含 --no-dashboard、--dashboard-port、--dashboard-host:`grep -c -- "--no-dashboard\|--dashboard-port\|--dashboard-host" <(help 输出)` 等价于人眼确认 0
    - `npm run chat`(或 `node dist/cli/index.js chat`)TUI 启动无「Cannot find module」「ReferenceError」等面板相关报错
    - 一轮对话完成:输入触发工具的请求 → 模型流式回复渲染 → 至少一次工具调用卡片显示并回显结果 → turn 结束
    - 输入 /dashboard 被安全拒绝(未知命令提示),不引发非空断言崩溃
    - /exit 退出无端口监听或未关闭句柄的报错
    - 人确认记录(approved + 摘要)写入 01-02-SUMMARY.md
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 静态验证 → 运行时 | 01-01 的 typecheck/build 不能捕获动态 import 在运行时的 ESM 解析失败;只有真跑 TUI 才能证明面板切除无运行时残留 |
| i18n 契约 → locale 实现 | types.ts 删键后,5 个 locale 必须同步;多删少留都会被 tsc 捕获,但语义错配(键留值错)需要人眼或运行时核对 |
| 用户输入 → 已下线命令 | /dashboard 命令在 01-01 已从注册表移除;运行时输入该命令应安全拒绝,不应触发非空断言崩溃 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Denial of Service (D) | src/cli/ui/App.tsx 残留动态 import 的运行时解析 | high | mitigate | Task 2 的人交互冒烟专门捕获「Cannot find module .../server/index.js」类 ESM 解析失败。typecheck 看不到动态 import,只有运行 TUI 才能证明。 |
| T-02-02 | Tampering (T) | /dashboard 命令残留注册的运行时崩溃 | high | mitigate | Task 2 步骤 2.e 让人主动输入 /dashboard 验证安全拒绝;若仍崩溃则回到 01-01 Task 3 补齐 commands.ts/dispatch.ts 同步删除。 |
| T-02-03 | Repudiation (R) | i18n 键集不一致导致的运行时 undefined 渲染 | medium | mitigate | Task 1 的 tsc 类型检查 + 5 locale 同步删除保证契约一致;Task 2 的 TUI 启动观察捕获运行时 undefined 文案。 |
| T-02-SC | Tampering (T) | npm 包安装 | low | accept | 本计划无任何新依赖安装(i18n 文件改造 + 人交互验证)。Package Legitimacy Audit 不适用。 |

</threat_model>

<verification>
计划级验证:
- Task 1 自动化:`npm run typecheck && npm run build && npm run lint` 三件套通过;i18n 面板键 grep 归零;收敛基线快照记录入 SUMMARY
- Task 2 人交互:`reasonix code --help` 无面板选项、`reasonix chat` TUI 启动无动态 import 失败、一轮对话(消息→回复→工具)完成、/dashboard 安全拒绝、退出无句柄泄漏
- 综合:Phase 1 四条 ROADMAP 成功标准全部满足(目录移除+无引用、CLI 适配代码清除、TUI 一轮对话、typecheck+build 通过)
</verification>

<success_criteria>
- src/i18n/ 下面板专用键清零,types.ts 与 5 locale 键集一致,tsc 通过
- reasonix chat TUI 经人确认完成一轮对话,无面板相关报错
- reasonix code --help 不再列面板相关 CLI 选项
- Phase 1 收尾:Web 面板表面与 CLI 内部面板适配代码完全切除,核心零回归红线得到静态 + 运行时双重证据
</success_criteria>

## Artifacts this phase produces
- 改造后的 `src/i18n/types.ts`(移除面板专用键声明)
- 改造后的 `src/i18n/{EN,zh-CN,JA,de,ru}.ts`(5 locale 同步移除对应值)
- 改造后的 `src/cli/ui/markdown.tsx`(顶部叙事注释中性化)
- 01-02-SUMMARY.md(含收敛基线 grep 快照与人确认记录)

## Artifacts this phase removes
- i18n 契约键(在 types.ts):`ui.dashboardPortHint`、`ui.dashboardPortInvalid`、`ui.noDashboard`、`ui.dashboardAutoStartFailed`、`handlers.dashboard.*` 子段(notAvailable/stopNoCallback/notRunning/stopping/copied/tokenReset/tokenResetting/ready/readyHint/failed/alreadyRunning/alreadyRunningHint)、`statusDash`、以及 01-01 之后仍残留的其他面板专用键(以实际 grep 为准)
- 5 个 locale 文件中对应上述键的值字符串
- src/cli/ui/markdown.tsx 顶部关于面板 CSS 的叙事性引用
- 收敛 pass 注意:以上被移除的 i18n 键不要被 drift 检查器误报为意外删除 —— 它们是本计划有意移除的面板残骸。

<output>
完成后创建 `.planning/phases/01-web-panel-removal/01-02-SUMMARY.md`(含收敛基线 grep 快照与 TUI 冒烟人确认记录)
</output>
