# Stack — Current

- Node.js 22+, TypeScript strict ESM
- Commander CLI, React + in-tree Ink terminal renderer
- Vitest with V8 coverage, Biome, `tsc --noEmit`, tsup
- DeepSeek API; optional MCP and QQ/Telegram/Weixin SDK integrations
- npm workspaces for `packages/*`

No Tauri, Rust desktop shell, Vite browser dashboard, or browser runtime is part of the current product/build.
