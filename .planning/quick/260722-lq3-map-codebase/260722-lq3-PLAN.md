---
quick: 260722-lq3
phase: quick-260722-lq3
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/codebase/STACK.md
  - .planning/codebase/INTEGRATIONS.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/CONVENTIONS.md
  - .planning/codebase/TESTING.md
  - .planning/codebase/CONCERNS.md
  - .planning/intel/file-roles.json
  - .planning/intel/api-map.json
  - .planning/intel/dependency-graph.json
  - .planning/intel/arch-decisions.json
  - .planning/intel/stack.json
  - .planning/intel/API-SURFACE.md
  - .planning/intel/.last-refresh.json
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "All seven codebase map documents describe the current repository and each contains more than 20 lines of evidence-based content with real file paths."
    - "The map refresh finishes before any Intel refresh begins; Intel is rebuilt from the refreshed repository/map state."
    - "Current-state artifacts identify reasonix-legacy as the sole public package/CLI identity and contain no active reference to the removed dsnix workspace or old reasonix command identity."
    - "All five canonical Intel JSON files are structurally valid, freshly timestamped, and represented by matching SHA-256 hashes in .last-refresh.json."
    - "API-SURFACE.md is regenerated from the refreshed api-map.json and is non-empty."
    - "Generated map documents pass the workflow's credential-pattern scan before they are committed."
  artifacts:
    - ".planning/codebase/{STACK,INTEGRATIONS,ARCHITECTURE,STRUCTURE,CONVENTIONS,TESTING,CONCERNS}.md"
    - ".planning/intel/{file-roles,api-map,dependency-graph,arch-decisions,stack}.json"
    - ".planning/intel/API-SURFACE.md"
    - ".planning/intel/.last-refresh.json"
  key_links:
    - "package.json and live source/config/tests -> four mapper focus passes -> seven codebase map documents"
    - "verified seven-document map + live repository -> full gsd-intel-updater run -> five canonical Intel JSON files"
    - "api-map.json -> gsd-tools intel api-surface -> API-SURFACE.md"
    - "five canonical Intel JSON files -> gsd-tools intel snapshot -> matching hashes in .last-refresh.json"
---

<objective>
Fully refresh the repository's codebase map, then refresh the enabled Intel store from that verified current-state map.

Purpose: The existing map is terse and the Intel store is stale: it still describes the removed `packages/dsnix` workspace and the retired `reasonix` binary identity. Planning must again have trustworthy, current repository context without changing product code or ROADMAP.md.

Output: Seven refreshed `.planning/codebase/*.md` documents followed by five refreshed Intel JSON files, a regenerated API surface, and a hash-backed refresh snapshot.
</objective>

<execution_context>
@C:/Users/zhangyujia/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/zhangyujia/.codex/gsd-core/templates/summary.md
@C:/Users/zhangyujia/.codex/skills/gsd-map-codebase/SKILL.md
@C:/Users/zhangyujia/.codex/gsd-core/workflows/map-codebase.md
@C:/Users/zhangyujia/.codex/agents/gsd-intel-updater.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/config.json
@package.json

# Current facts that the refresh must reflect
- `package.json` is `reasonix-legacy@1.3.1` and exposes only the `reasonix-legacy` bin.
- `packages/dsnix` does not exist.
- `.planning/config.json` has `intel.enabled: true`.
- The runtime has four collaboration slots total, including the executor. Never run four child mappers at once.
- Preserve unrelated work. Refresh only the listed map/Intel artifacts and the executor-created SUMMARY; do not touch `.planning/ROADMAP.md` or product source.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh technology and architecture map documents</name>
  <files>.planning/codebase/STACK.md, .planning/codebase/INTEGRATIONS.md, .planning/codebase/ARCHITECTURE.md, .planning/codebase/STRUCTURE.md</files>
  <action>
Record the starting `git status --short` and HEAD so unrelated changes can be distinguished from this quick task. Do not delete `.planning/codebase/` wholesale; overwrite only this task's four owned documents.

Run the first bounded mapper wave with two typed `gsd-codebase-mapper` children in parallel, following the prompts and templates in `map-codebase.md`:

1. `tech` focus owns `STACK.md` and `INTEGRATIONS.md`.
2. `arch` focus owns `ARCHITECTURE.md` and `STRUCTURE.md`.

Both mappers perform a full-repository scan (no `--paths` scope), exclude planning artifacts, generated outputs, dependencies, VCS metadata, and forbidden credential/key files, and write documents directly. Require evidence from live files such as `package.json`, entry points, configs, workflows, source modules, and directory structure; every architectural or integration claim must cite actual repository paths. Describe current state only. Use the current package manifest as authority for the sole public CLI identity and the live `packages/` tree as authority for workspace membership. Historical removals may inform the scan but must not be presented as live paths or executable commands.

Wait for both agents, collect their `## Mapping Complete` confirmations and line counts, and fail this task if either assigned file is absent, has 20 or fewer lines, or lacks path-backed evidence. Do not begin the second mapper wave while either child remains active.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const files=['STACK.md','INTEGRATIONS.md','ARCHITECTURE.md','STRUCTURE.md'].map(x=>'.planning/codebase/'+x);const bad=[];for(const f of files){if(!fs.existsSync(f)){bad.push(f+': missing');continue}const t=fs.readFileSync(f,'utf8');const n=t.split(/\r?\n/).length;if(n&lt;=20)bad.push(f+': '+n+' lines');if(!/`(?:src|packages|tests|scripts|docs|\.github|package\.json)/.test(t))bad.push(f+': no path-backed evidence')}if(bad.length)throw Error(bad.join('; '));console.log('OK: four tech/arch map documents are complete')"</automated>
  </verify>
  <done>
  - Tech and architecture mapper confirmations were received with no ownership collision.
  - STACK, INTEGRATIONS, ARCHITECTURE, and STRUCTURE each exceed 20 lines and cite live repository paths.
  - These documents describe the current CLI-only repository, current dependencies/workspaces, and current `reasonix-legacy` identity.
  - No unrelated file was modified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Complete and security-check the seven-document codebase map</name>
  <files>.planning/codebase/CONVENTIONS.md, .planning/codebase/TESTING.md, .planning/codebase/CONCERNS.md</files>
  <action>
Only after Task 1 and both of its mapper children have completed, run the second bounded mapper wave with two typed `gsd-codebase-mapper` children in parallel:

1. `quality` focus owns `CONVENTIONS.md` and `TESTING.md`.
2. `concerns` focus owns `CONCERNS.md`.

Use a full-repository scan with the same exclusions and current-state discipline as Task 1. The quality mapper must derive conventions and test commands from the actual TypeScript/Biome/Vitest/tsup configuration and representative live source/tests. The concerns mapper must separate live risks from archived history and cite the exact files or subsystem boundaries supporting every concern. Wait for both `## Mapping Complete` confirmations.

Then verify the complete seven-document set as one atomic map: exact filenames present, every document over 20 lines, actual path citations present, current CLI identity aligned with `package.json`, and no stale removed-workspace or retired-command claims. Run the credential-pattern scan from `map-codebase.md` across all seven generated documents. If it flags anything, stop before commit and inspect/redact the generated prose; never waive a real credential. Compare `git status --short` with the starting snapshot and leave ROADMAP.md, source, config, and all unrelated user work untouched.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const dir='.planning/codebase';const names=['STACK.md','INTEGRATIONS.md','ARCHITECTURE.md','STRUCTURE.md','CONVENTIONS.md','TESTING.md','CONCERNS.md'];const secret=/(sk-[A-Za-z0-9]{20,}|sk_(?:live|test)_[A-Za-z0-9]+|gh[po]_[A-Za-z0-9]{36}|glpat-[A-Za-z0-9_-]+|AKIA[A-Z0-9]{16}|xox[baprs]-[A-Za-z0-9-]+|-----BEGIN.*PRIVATE KEY|eyJ[A-Za-z0-9_-]+\.eyJ[A-Za-z0-9_-]+\.)/;const stale=/packages[\\/]+dsnix|(?&lt;![-\w])reasonix(?=\s+(?:setup|code|chat|run|acp|desktop|qq|telegram|weixin|stats|doctor|doctor-cache|commit|sessions|prune-sessions|events|replay|diff|mcp|version|update|index)\b)|bin(?:ary|aries)?\s*[:=]?\s*(?:reasonix(?!-legacy\b)|dsnix)\b/i;const bad=[];for(const n of names){const f=dir+'/'+n;if(!fs.existsSync(f)){bad.push(n+': missing');continue}const t=fs.readFileSync(f,'utf8');const lines=t.split(/\r?\n/).length;if(lines&lt;=20)bad.push(n+': '+lines+' lines');if(secret.test(t))bad.push(n+': secret-like material');if(stale.test(t))bad.push(n+': stale workspace/CLI identity');if(!/`(?:src|packages|tests|scripts|docs|\.github|package\.json)/.test(t))bad.push(n+': no path evidence')}if(bad.length)throw Error(bad.join('; '));console.log('OK: seven current, non-empty, path-backed map docs; identity and secret gates passed')"</automated>
  </verify>
  <done>
  - CONVENTIONS, TESTING, and CONCERNS each exceed 20 lines and cite live paths.
  - Exactly the seven required map documents form a complete, current technology/architecture/quality/concerns map.
  - No map document describes the deleted alias workspace or retired binary names as current state.
  - The generated map passes the secret-pattern scan with no real credential exposure.
  - The complete map refresh is finished and verified before Task 3 starts.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rebuild and snapshot all enabled Intel artifacts</name>
  <files>.planning/intel/file-roles.json, .planning/intel/api-map.json, .planning/intel/dependency-graph.json, .planning/intel/arch-decisions.json, .planning/intel/stack.json, .planning/intel/API-SURFACE.md, .planning/intel/.last-refresh.json</files>
  <action>
Do not start this task unless Task 2's complete-map verification passed. Confirm `intel.enabled` remains true, then spawn one typed `gsd-intel-updater` with `focus: full`. Its required reading must include `package.json`, `.planning/PROJECT.md`, all seven freshly written `.planning/codebase/*.md` documents, and the live source/config files needed to substantiate entries. The updater must follow its forbidden-file rules and return `## INTEL UPDATE COMPLETE`; do not accept a partial update.

Rebuild all five canonical JSON artifacts from current evidence: `file-roles.json`, `api-map.json`, `dependency-graph.json`, `arch-decisions.json`, and `stack.json`. Each must have a fresh ISO `_meta.updated_at`, incremented integer `_meta.version`, valid schema, actual exported symbol names, existing path references, and current package/dependency/CLI facts. Removed files must disappear rather than be preserved from the old store.

After the agent returns, use `C:/Users/zhangyujia/.codex/gsd-core/bin/gsd-tools.cjs` to run `intel validate`, then `intel api-surface`, then `intel snapshot` in that order. The API-surface command must regenerate `.planning/intel/API-SURFACE.md` from the new `api-map.json`; the snapshot command must write `.last-refresh.json` itself. This schema self-check is part of the Intel refresh, not a separate GSD validate phase. Require `intel status --raw` to report `overall_stale: false` and `intel diff --raw` to report no changed, added, or removed canonical Intel files immediately after the snapshot.

Finally, parse every JSON file, recompute SHA-256 for the five canonical files, compare every digest with `.last-refresh.json`, verify fresh metadata, ensure API-SURFACE is non-empty, and apply the same removed-workspace/retired-command identity gate to all Intel artifacts. Preserve unrelated work and do not modify ROADMAP.md.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),crypto=require('crypto'),cp=require('child_process');const cli='C:/Users/zhangyujia/.codex/gsd-core/bin/gsd-tools.cjs';const run=(a)=>JSON.parse(cp.execFileSync(process.execPath,[cli,'intel',a,'--raw'],{encoding:'utf8'}));const valid=run('validate');if(!valid.valid)throw Error('intel validate failed: '+JSON.stringify(valid.errors));const status=run('status');if(status.overall_stale)throw Error('intel status remains stale');const diff=run('diff');if(['changed','added','removed'].some(k=>(diff[k]||[]).length))throw Error('snapshot mismatch: '+JSON.stringify(diff));const dir='.planning/intel',names=['file-roles.json','api-map.json','dependency-graph.json','arch-decisions.json','stack.json'];const snap=JSON.parse(fs.readFileSync(dir+'/.last-refresh.json','utf8'));const stale=/packages[\\/]+dsnix|(?&lt;![-\w])reasonix(?=\s+(?:setup|code|chat|run|acp|desktop|qq|telegram|weixin|stats|doctor|doctor-cache|commit|sessions|prune-sessions|events|replay|diff|mcp|version|update|index)\b)|bin(?:ary|aries)?\s*[:=]?\s*(?:reasonix(?!-legacy\b)|dsnix)\b/i;const bad=[];for(const n of names){const p=dir+'/'+n,j=JSON.parse(fs.readFileSync(p,'utf8')),h=crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex');if(!j._meta||!Number.isInteger(j._meta.version)||!Date.parse(j._meta.updated_at)||Date.now()-Date.parse(j._meta.updated_at)&gt;3600000)bad.push(n+': stale/invalid metadata');if(snap.hashes?.[n]!==h)bad.push(n+': hash mismatch');if(stale.test(fs.readFileSync(p,'utf8')))bad.push(n+': stale workspace/CLI identity')}const keys=Object.keys(snap.hashes||{}).sort();if(JSON.stringify(keys)!==JSON.stringify([...names].sort()))bad.push('.last-refresh.json: hash set is not exactly five canonical files');if(!Date.parse(snap.timestamp)||Date.now()-Date.parse(snap.timestamp)&gt;3600000)bad.push('.last-refresh.json: stale timestamp');const surface=fs.readFileSync(dir+'/API-SURFACE.md','utf8');if(surface.split(/\r?\n/).length&lt;=20)bad.push('API-SURFACE.md: empty/incomplete');if(stale.test(surface))bad.push('API-SURFACE.md: stale CLI identity');if(bad.length)throw Error(bad.join('; '));console.log('OK: full Intel refresh valid, current, rendered, and hash-backed')"</automated>
  </verify>
  <done>
  - The updater returned `## INTEL UPDATE COMPLETE` after a full refresh based on the verified new map.
  - All five Intel JSON files parse, validate, carry fresh metadata, reference current repository facts, and omit removed file entries.
  - API-SURFACE.md is regenerated and non-empty.
  - `.last-refresh.json` contains exactly five current hashes and a fresh timestamp; `intel status` and `intel diff` confirm the snapshot is current.
  - No Intel artifact retains the removed alias workspace or retired command identity as an active surface.
  - ROADMAP.md and unrelated work remain untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository files -> mapper/updater context | Agents inspect a large, partly untrusted working tree; forbidden secret/key files and generated/vendor directories must not enter generated planning artifacts. |
| Mapper outputs -> Intel updater | Intel must consume only the completed, verified current map; partial or stale map output would contaminate every downstream index. |
| Intel JSON -> planning/query consumers | Machine-readable data and API-SURFACE are trusted by later planners, so stale paths, invented exports, or mismatched snapshot hashes can misdirect implementation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick-lq3-01 | Information Disclosure | `.planning/codebase/*.md` generation | high | mitigate | Mappers skip forbidden credential/key files; Task 2 runs the workflow's concrete token/private-key regex over every generated map document and blocks on a real hit. |
| T-quick-lq3-02 | Tampering | map-to-Intel handoff | medium | mitigate | Task 3 is ordered after the seven-document completeness/current-identity gate and cannot start on partial mapper output. |
| T-quick-lq3-03 | Spoofing | package and CLI identity facts | medium | mitigate | `package.json` and live workspace paths are authoritative; automated gates reject removed-workspace and retired-command claims in both map and Intel artifacts. |
| T-quick-lq3-04 | Tampering | `.last-refresh.json` | medium | mitigate | Generate the snapshot only through `gsd-tools intel snapshot`, then independently recompute and compare all five SHA-256 digests. |
| T-quick-lq3-05 | Denial of Service | mapper concurrency | low | mitigate | Use two bounded child-agent waves and wait between them, staying below the four-slot runtime ceiling. |
| T-quick-lq3-06 | Repudiation | unrelated working-tree changes | low | mitigate | Capture starting status/HEAD, restrict file ownership to the declared artifacts, and preserve ROADMAP.md and all unrelated changes. |
</threat_model>

<source_coverage>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Fully refresh `.planning/codebase/`, then refresh Intel from the updated state | Tasks 1-2 complete and verify all seven map documents; Task 3 has an explicit prerequisite on that verification and performs the full Intel rebuild. |
| REQ | No ROADMAP requirement IDs apply to this quick task | Excluded by quick-task scope; `requirements: []` and ROADMAP.md is explicitly protected. |
| RESEARCH | No research artifact or research phase requested | Excluded per locked quick-task constraint; live repository evidence and workflow contracts are sufficient. |
| CONTEXT | Cover all seven map documents | Tasks 1-2 cover tech, arch, quality, and concerns focuses with exact file ownership. |
| CONTEXT | Cover all enabled Intel artifacts and refresh hashes/metadata | Task 3 covers five JSON files, API-SURFACE.md, and `.last-refresh.json` with schema, freshness, and digest checks. |
| CONTEXT | Detect stale dsnix workspace and old reasonix binary identity | Task 2 gates the map; Task 3 gates every Intel artifact. |
| CONTEXT | Secret-scan generated map docs | Task 2 implements the map workflow's credential-pattern gate. |
| CONTEXT | Respect four collaboration slots while retaining four mapper focuses | Tasks 1-2 use two bounded two-agent waves with distinct tech/arch/quality/concerns ownership. |
| CONTEXT | Preserve unrelated work; do not touch ROADMAP.md | Every task scopes writes and checks against the captured starting status. |

All goal/context items are covered. No source item is missing, deferred, or reduced.
</source_coverage>

<verification>
- Task order is mandatory: tech/arch map wave -> quality/concerns map wave and full map gate -> full Intel refresh.
- All seven map documents exist, exceed 20 lines, cite live paths, reflect `reasonix-legacy`, and pass the credential-pattern scan.
- `node C:/Users/zhangyujia/.codex/gsd-core/bin/gsd-tools.cjs intel validate --raw` returns `valid: true`.
- `intel status --raw` returns `overall_stale: false`; `intel diff --raw` is empty immediately after snapshot.
- Snapshot digests independently match the five canonical Intel JSON files; API-SURFACE.md is regenerated and non-empty.
- Removed-workspace and retired-command identity patterns are absent from current map and Intel artifacts.
- Final diff contains only the declared codebase/Intel artifacts plus the quick SUMMARY; `.planning/ROADMAP.md` is unchanged.
</verification>

<success_criteria>
- The repository has a useful, evidence-backed seven-document map across technology, architecture, quality, testing, integrations, structure, and concerns.
- Intel is rebuilt only after that map is complete and reflects the same current repository/package identity.
- All generated data passes completeness, structure, freshness, hash, stale-reference, and map-secret gates.
- No research phase, GSD validate phase, product-code edit, ROADMAP change, or unrelated-work overwrite occurs.
</success_criteria>

<output>
Create `.planning/quick/260722-lq3-map-codebase/260722-lq3-SUMMARY.md` when done.
</output>
