---
quick: 260706-ne6
phase: quick-260706-ne6
plan: "01"
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [IN-01, IN-02, IN-03, IN-04, IN-05, IN-06, WR-03, WR-04]
files_modified:
  - src/cli/headless/gate-parsers.ts
  - src/cli/headless/gate-bridges.ts
  - src/cli/commands/qq.ts
  - src/cli/commands/telegram.ts
  - src/cli/commands/weixin.ts
  - src/qq/use-qq-channel.ts
  - src/telegram/use-telegram-channel.ts
  - src/weixin/use-weixin-channel.ts
  - tests/headless-gate-bridges.test.ts
  - tests/weixin-command.test.ts

must_haves:
  truths:
    - "IN-01: `reasonix qq|telegram|weixin` still honor --effort (persisted upstream by persistEffortFlag in cli/index.ts); only the never-read `effort?` interface field is removed."
    - "IN-02: a gate prompt is built exactly once, bridge-side, and surfaced via sendPrompt(promptText: string) — the double defaultBuildPrompt call and `void prompt` are gone."
    - "IN-03: qq/telegram/weixin no longer pass an all-no-op gateCallbacks bag; the bridge accepts gateCallbacks as optional."
    - "IN-04: plan_checkpoint 'revise' resolves with a canonical CheckpointVerdict ({type:'revise', feedback}) — no extraneous `checkpoint` field."
    - "IN-05: consumeSlashReply in all three use-*-channel hooks has no unused `lowerText` binding."
    - "IN-06: parse*Choice + stripFollowupPrefix live in one React-free module src/cli/headless/gate-parsers.ts, imported by gate-bridges.ts and all three hooks (single source of truth)."
    - "WR-03: two concurrent pause requests in one turn both resolve — the bridge queues pendings FIFO instead of clobbering a single slot."
    - "WR-04: Ctrl-C during the weixin QR-login window runs host.shutdown() and exits cleanly — SIGINT/SIGTERM handlers are installed BEFORE runWeixinQrLogin."
  artifacts:
    - "src/cli/headless/gate-parsers.ts (new, React-free shared parsers)"
    - "src/cli/headless/gate-bridges.ts (sendPrompt signature, optional gateCallbacks, dropped checkpoint field, FIFO queue, parser imports)"
    - "src/cli/commands/{qq,telegram,weixin}.ts (drop effort field + no-op gateCallbacks + defaultBuildPrompt import; simplified sendPrompt)"
    - "src/qq/use-qq-channel.ts, src/telegram/use-telegram-channel.ts, src/weixin/use-weixin-channel.ts (import shared parsers, drop local copies + lowerText)"
    - "tests/headless-gate-bridges.test.ts (sendPrompt + parser-import updates; WR-03 concurrent-pending regression case)"
    - "tests/weixin-command.test.ts (WR-04 QR-window SIGINT regression case)"
  key_links:
    - "gate-parsers.ts exports parseIndexedChoice/parseRunPermissionChoice/parsePlanChoice/parseCheckpointChoice/parseRevisionChoice/stripFollowupPrefix + type ReviseChoice; gate-bridges.ts:107-153 local copies deleted and imported from ./gate-parsers.js."
    - "gate-bridges.ts installHeadlessGateBridges: single-slot `let pending` (was :344) → `const pendingQueue: PendingInteraction[]`; consumeReply (was :380-385) shifts FIFO."
    - "gate-bridges.ts:365-377 bridge builds prompt via (opts.buildPrompt ?? defaultBuildPrompt) then opts.sendPrompt(prompt); `void prompt` removed; HeadlessGateBridgeOptions.sendPrompt is now (promptText: string) => void."
    - "gate-bridges.ts:273-277 revise verdict drops `checkpoint:{...}`; CheckpointVerdict (packages/core-utils/src/permission-types.ts:15-18) has no such field."
    - "weixin.ts: cleanup closure + process.on(SIGINT/SIGTERM) relocated ABOVE runWeixinQrLogin (was QR at :80, handlers at :205-206); bridge/channel declared before QR and optional-chained in cleanup."
---

<objective>
Close the Phase-2 code-review deferred follow-ups (GitHub issue #4): dead-code / interface cleanups IN-01..IN-06 plus the two small reliability bugs WR-03 (single-slot gate `pending` clobbers concurrent pauses) and WR-04 (weixin QR-login window has no SIGINT handler). WR-01 already shipped with issue #2 and is excluded.

Purpose: remove rot flagged in .planning/phases/02-bot-decoupling-to-standalone-cli/02-REVIEW.md before it misleads maintainers, and fix two real (small) bugs — a gate that hangs on parallel tool pauses and an uninterruptible multi-minute QR window.

Output: one new React-free shared module (gate-parsers.ts), tidied gate-bridge + three bot command controllers + three channel hooks, and two new regression tests. Full relevant test surface stays green.

Scope boundary (issue #5, NOT here): do NOT extract a BaseChannelAdapter and do NOT dedup the parallel qq/telegram/weixin command bodies. IN-06's gate-parsers.ts extraction IS in scope.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-bot-decoupling-to-standalone-cli/02-REVIEW.md

# Files under change (read before editing)
@src/cli/headless/gate-bridges.ts
@src/cli/commands/qq.ts
@src/cli/commands/telegram.ts
@src/cli/commands/weixin.ts
@src/qq/use-qq-channel.ts
@src/telegram/use-telegram-channel.ts
@src/weixin/use-weixin-channel.ts
@tests/headless-gate-bridges.test.ts
@tests/weixin-command.test.ts

# Contracts referenced (read-only)
@src/core/pause-gate.ts
@packages/core-utils/src/permission-types.ts
@src/cli/index.ts
</context>

<source_coverage>
Every issue-#4 item maps to a task. Source = 02-REVIEW.md Info/Warning sections (the authoritative issue list).

| Item | What | Task |
|------|------|------|
| IN-01 | dead `effort?` field in 3 *CommandOptions | Task 1 |
| IN-02 | double defaultBuildPrompt + `void prompt` | Task 1 |
| IN-03 | all-no-op gateCallbacks bag in 3 commands | Task 1 |
| IN-04 | undocumented `checkpoint` field on revise verdict | Task 1 |
| IN-05 | unused `lowerText` in 3 hooks' consumeSlashReply | Task 2 |
| IN-06 | parse*Choice duplicated across 4 modules | Task 2 |
| WR-03 | single-slot `pending` clobbers concurrent gates | Task 3 |
| WR-04 | weixin QR window runs before SIGINT handlers | Task 3 |

Excluded (out of scope / already done): WR-01 (shipped with issue #2), WR-02 (already fixed — word-boundary parsers present in all 4 copies), WR-05/WR-06 (deferred, not in this issue), IN-06 command-body dedup + BaseChannelAdapter (issue #5).
</source_coverage>

<tasks>

<task type="auto">
  <name>Task 1: IN-01..IN-04 — dead-code + gate-bridge interface tidy</name>
  <files>src/cli/headless/gate-bridges.ts, src/cli/commands/qq.ts, src/cli/commands/telegram.ts, src/cli/commands/weixin.ts, tests/headless-gate-bridges.test.ts</files>
  <action>
Four cleanups; no behavior change except removing wasted work and a type-drift field.

IN-01 — remove the never-read `effort?: string;` field (and its one-line doc comment) from QqCommandOptions (qq.ts ~28-29), TelegramCommandOptions (telegram.ts ~32-33), WeixinCommandOptions (weixin.ts ~43-44). Confirmed safe: cli/index.ts qq/telegram/weixin `.action` handlers call persistEffortFlag(opts.effort) + resolveDefaults, but pass ONLY {model, workspace, budgetUsd} into the command functions — effort is never forwarded and never read in the bodies. Do not touch cli/index.ts.

IN-02 — make the bridge the single prompt builder and change the sendPrompt contract to receive the built text (per the anchor: "have the bridge build once and pass the text to sendPrompt(promptText)"). In gate-bridges.ts:
  - Change HeadlessGateBridgeOptions.sendPrompt to `sendPrompt(promptText: string): void` (update its doc line to say it receives the already-built prompt text).
  - In installHeadlessGateBridges step (3) (~365-377): keep `const prompt = (opts.buildPrompt ?? defaultBuildPrompt)(req.kind, req.payload as Record<string, unknown>);`, keep stashing pending, then call `opts.sendPrompt(prompt);`. Delete the `void prompt;` line and its two-line explanatory comment.
In each command (qq.ts ~76-87, telegram.ts ~79-90, weixin.ts ~106-117): replace the sendPrompt closure body so it no longer calls defaultBuildPrompt; it now receives promptText and forwards it, preserving the empty-guard and the existing redacted catch:
  `sendPrompt: (promptText) => { if (promptText) { void channel?.sendResponse(promptText).catch((err) => { process.stderr.write(t("commands.<ch>.sendFailed", { msg: redactSecretsInText((err as Error).message, botSecrets) })); }); } }`
Then remove `defaultBuildPrompt` from each command's import from "../headless/gate-bridges.js" (keep installHeadlessGateBridges). defaultBuildPrompt stays exported from gate-bridges.ts (bridge + gate-bridges test still use it).

IN-03 — make gateCallbacks optional. In gate-bridges.ts: change HeadlessGateBridgeOptions.gateCallbacks to `gateCallbacks?: Partial<GateCallbacks>;`; change dispatchReply's param to `gateCallbacks?: Partial<GateCallbacks>` and convert every `gateCallbacks.onXxx(...)` call to optional-chained `gateCallbacks?.onXxx?.(...)` (onShellConfirm, onPathConfirm, onPlanCancel, onPlanFeedback, onCheckpointConfirm, onCheckpointRevise, onPlanRevision, onChoiceResolve). installHeadlessGateBridges still passes `opts.gateCallbacks` into consumeReply's dispatchReply call (now possibly undefined — fine). In all three commands: delete the entire `gateCallbacks: { ...eight () => undefined... }` property so the install call is just `installHeadlessGateBridges({ sendPrompt: ... })`.

IN-04 — in gate-bridges.ts dispatchReply plan_checkpoint 'revise' branch (~273-277), drop the `checkpoint: { stepId: ..., title: ... }` property from the pauseGate.resolve object so it is the canonical `{ type: "revise", feedback: followup }`. CheckpointVerdict (packages/core-utils/src/permission-types.ts:15-18) has no `checkpoint` field and the consuming tool reads only .type/.feedback. Leave the separate `gateCallbacks?.onCheckpointRevise?.(followup, { stepId, title })` call intact (it legitimately carries the snapshot).

Test update (tests/headless-gate-bridges.test.ts) for the IN-02 signature change ONLY: in runInteractiveReplyResolveCase, change `sendPrompt: (kind, _payload) => { sendPromptCalls++; lastPromptKind = kind; }` to capture the text — e.g. `let lastPrompt = ""; sendPrompt: (promptText) => { sendPromptCalls++; lastPrompt = promptText; }` — and replace the `assert.equal(lastPromptKind, "run_command")` assertion with `assert.ok(lastPrompt.includes("echo hi"), "sendPrompt receives the built prompt text")` (the ask payload command is "echo hi"). The other cases' sendPrompt closures take no args and stay as-is. Leave each case's gateCallbacks bag unchanged — it still type-checks under Partial<GateCallbacks> and the onShellConfirm spies are how the cases observe dispatch.

Comment policy: do not add /* */ blocks or "Phase N" text; keep any new note to one `//` line.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npx vitest run tests/headless-gate-bridges.test.ts tests/qq-command.test.ts tests/telegram-command.test.ts tests/weixin-command.test.ts</automated>
    <automated>npx @biomejs/biome check src/cli/headless/gate-bridges.ts src/cli/commands/qq.ts src/cli/commands/telegram.ts src/cli/commands/weixin.ts</automated>
  </verify>
  <done>
`effort` gone from all three *CommandOptions; sendPrompt is (promptText: string) => void and defaultBuildPrompt is called once (bridge-side); no `void prompt`; commands import only installHeadlessGateBridges; gateCallbacks is optional and dispatchReply optional-chains it; revise verdict has no `checkpoint` field. tsc clean, the four listed test files pass, biome clean on the touched files. Suggested commit: `refactor(headless): drop dead effort option, double prompt build, no-op gate callbacks, checkpoint field (IN-01..04)`.
  </done>
</task>

<task type="auto">
  <name>Task 2: IN-05 + IN-06 — extract shared React-free gate-parsers, drop unused lowerText</name>
  <files>src/cli/headless/gate-parsers.ts, src/cli/headless/gate-bridges.ts, src/qq/use-qq-channel.ts, src/telegram/use-telegram-channel.ts, src/weixin/use-weixin-channel.ts, tests/headless-gate-bridges.test.ts</files>
  <action>
IN-06 — create src/cli/headless/gate-parsers.ts: a pure, React-free module (zero React imports so the headless graph stays React-free). Give it a <=2-line `//` header. Export, verbatim from the current gate-bridges.ts copies, these six functions plus the ReviseChoice type:
  - `export type ReviseChoice = "accept" | "reject";`
  - `export function parseIndexedChoice(text: string): number` (the `text.match(/^(\d+)/)` form)
  - `export function parseRunPermissionChoice(text: string): "run_once" | "always_allow" | "deny"` (the word-boundary `\b...\b` form already present — this is the WR-02-fixed version)
  - `export function parsePlanChoice(text: string): "approve" | "refine" | "cancel"`
  - `export function parseCheckpointChoice(text: string): "continue" | "revise" | "stop"`
  - `export function parseRevisionChoice(text: string): ReviseChoice | "cancel"`
  - `export function stripFollowupPrefix(text: string): string`
These are byte-identical across gate-bridges.ts and all three hooks (verified — no drift), so this is a no-op refactor: move, don't rewrite logic. Do NOT include isCancelText/isNewText — those are hook-local slash helpers, not gate parsers, and stay in the hooks.

Rewire gate-bridges.ts: delete its local definitions of the six functions (was ~107-153) and its local `export type ReviseChoice` (~29) plus that type's comment; import them instead: add `import { parseIndexedChoice, parseRunPermissionChoice, parsePlanChoice, parseCheckpointChoice, parseRevisionChoice, stripFollowupPrefix, type ReviseChoice } from "./gate-parsers.js";`. Everything else in gate-bridges.ts (GateCallbacks using ReviseChoice, dispatchReply using the parsers) keeps working unchanged. Nothing outside the test imports these names from gate-bridges.js (verified), so no re-export is needed.

Rewire each hook (use-qq-channel.ts, use-telegram-channel.ts, use-weixin-channel.ts): delete the local `parseIndexedChoice`, `parseRunPermissionChoice`, `parsePlanChoice`, `parseCheckpointChoice`, `parseRevisionChoice`, `stripFollowupPrefix` (all six, all currently used — verified) and import them from the new module: `import { parseIndexedChoice, parseRunPermissionChoice, parsePlanChoice, parseCheckpointChoice, parseRevisionChoice, stripFollowupPrefix } from "../cli/headless/gate-parsers.js";`. KEEP each hook's local `isCancelText`/`isNewText`. KEEP each hook's existing `import type { ReviseChoice } from "../cli/ui/PlanReviseConfirm.js";` — it is still used by the onPlanRevision param type; the imported parseRevisionChoice returns gate-parsers' ReviseChoice which is the structurally identical `"accept" | "reject"`, so it is assignable to onPlanRevision with no change.

IN-05 — while in each hook, delete the single unused `const lowerText = text.toLowerCase();` line at the top of consumeSlashReply's callback (qq use-qq-channel.ts:514, telegram use-telegram-channel.ts:503, weixin use-weixin-channel.ts:532 — each appears exactly once and is never referenced; the next line already re-derives via parseIndexedChoice(text) / isCancelText(text)).

Test update (tests/headless-gate-bridges.test.ts): change the parseRunPermissionChoice import source from "../src/cli/headless/gate-bridges.js" to "../src/cli/headless/gate-parsers.js" (keep defaultBuildPrompt + installHeadlessGateBridges imported from gate-bridges.js). The runParseRunPermissionChoiceCase WR-02 assertions are unchanged.
  </action>
  <verify>
    <automated>test -f src/cli/headless/gate-parsers.ts</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>npx vitest run tests/headless-gate-bridges.test.ts tests/qq-channel-gate-callbacks.test.ts</automated>
    <automated>npx @biomejs/biome check src/cli/headless/gate-parsers.ts src/cli/headless/gate-bridges.ts src/qq/use-qq-channel.ts src/telegram/use-telegram-channel.ts src/weixin/use-weixin-channel.ts</automated>
  </verify>
  <done>
gate-parsers.ts exists and is imported by gate-bridges.ts and all three hooks; the six parse functions exist in exactly one place; isCancelText/isNewText remain hook-local; no `lowerText` binding remains in any consumeSlashReply. tsc clean (proves imports + ReviseChoice assignability), gate-bridges + qq-channel-gate-callbacks tests green (proves parse behavior preserved incl. the WR-02 deny-intent and "2 more detail" revise cases), biome clean. Suggested commit: `refactor(headless): extract shared gate-parsers module, drop unused lowerText (IN-05, IN-06)`.
  </done>
</task>

<task type="auto">
  <name>Task 3: WR-03 + WR-04 — queue concurrent gate pendings, guard weixin QR window</name>
  <files>src/cli/headless/gate-bridges.ts, src/cli/commands/weixin.ts, tests/headless-gate-bridges.test.ts, tests/weixin-command.test.ts</files>
  <action>
WR-03 — replace the bridge's single-slot pending with a FIFO queue so parallel pausing tools do not clobber each other (pauseGate itself is id-keyed via a Map and fully supports concurrent asks — verified in src/core/pause-gate.ts, so only the bridge is at fault). In installHeadlessGateBridges:
  - Replace `let pending: PendingInteraction | null = null;` (~344) with `const pendingQueue: PendingInteraction[] = [];`.
  - In the pauseGate.on step (3), replace the `pending = { gateId, kind, payload };` assignment with `pendingQueue.push({ gateId: req.id, kind: req.kind, payload: (req.payload as Record<string, unknown>) ?? {} });`.
  - Rewrite consumeReply (~380-385) to shift the oldest: `const consumeReply = (text: string): boolean => { const interaction = pendingQueue.shift(); if (!interaction) return false; return dispatchReply(interaction, text, opts.gateCallbacks); };`.
FIFO means the user's reply resolves the oldest outstanding gate first; no request is orphaned.

WR-03 regression test — add a case to tests/headless-gate-bridges.test.ts and register it in the `cases` array in run(). It fires two concurrent run_command asks in one session (review edit mode, so neither auto-resolves) and proves both resolve:
  wrap in `headlessContext.run("sess-concurrent", ...)` + `withTempHome("review", ...)`; `const ask1 = pauseGate.ask({ kind: "run_command", payload: { command: "echo one" } });` and `const ask2 = pauseGate.ask({ kind: "run_command", payload: { command: "echo two" } });`; `await Promise.resolve();`; then `assert.equal(consumeReply("1"), true)` twice; then `assert.deepEqual(await ask1, { type: "run_once" })` and `assert.deepEqual(await ask2, { type: "run_once" })`. Install the bridge with just `{ sendPrompt: () => undefined }` (relies on the now-optional gateCallbacks from Task 1). Against the pre-fix single-slot code ask1 would never resolve (the test would hang), so this is a true regression guard.

WR-04 — install the weixin cleanup + signal handlers BEFORE runWeixinQrLogin so Ctrl-C during the multi-minute QR window tears down gracefully. Restructure weixinCommand (src/cli/commands/weixin.ts) after `host = await HeadlessHost.create(...)`:
  - Declare the mutable bindings up front, before the QR block: `let channel: WeixinChannel | null = null;`, `let bridge: InstalledHeadlessGateBridge | null = null;`, `let cleaningUp = false;`.
  - Define the cleanup closure here (before QR), null-safe for the not-yet-constructed channel/bridge: `const cleanup = async (): Promise<void> => { if (cleaningUp) return; cleaningUp = true; bridge?.unsubscribe(); try { await channel?.stop(); } catch { /* best-effort teardown */ } finally { host.shutdown(); process.exit(0); } };` then `process.on("SIGINT", () => void cleanup()); process.on("SIGTERM", () => void cleanup());`.
  - Then run the existing QR-login-if-missing block, then the botSecrets snapshot (keep it AFTER the QR persist), then assign `bridge = installHeadlessGateBridges({ sendPrompt: ... })` (the Task 1 simplified form) into the outer `bridge`, then assign `channel = new WeixinChannel({...})` into the outer `channel`, then `await channel.start();`.
  - Delete the now-duplicate cleanup + process.on block that currently sits after channel construction (~189-206), and remove the earlier local `let channel`/`let cleaningUp` redeclarations so there is exactly one of each.
  - Add `type InstalledHeadlessGateBridge` to the gate-bridges.js import (alongside installHeadlessGateBridges; defaultBuildPrompt already removed in Task 1).

WR-04 regression test — add an `it(...)` to tests/weixin-command.test.ts proving a SIGINT during the pending QR window shuts the host down before any channel exists. Set `loadWeixinConfigMock.mockReturnValue({ token: undefined, accountId: undefined, enabled: false })` to force the QR path; make QR hang with a deferred: `let releaseQr: (v: unknown) => void = () => undefined; runWeixinQrLoginMock.mockReturnValue(new Promise((res) => { releaseQr = res; }));`. Start `const promise = weixinCommand({ workspace: tmpWorkspace })` (don't await); drain a few `await Promise.resolve()`; assert `runWeixinQrLoginMock` was called and `channelStartMock` was NOT (still inside the QR window); `process.emit("SIGINT", "SIGINT")`; drain microtasks; then assert `const fakeHost = await hostCreateMock.mock.results[0]?.value; expect(fakeHost?.shutdown).toHaveBeenCalled();` and `expect(exitSpy).toHaveBeenCalledWith(0)` and `expect(channelStartMock).not.toHaveBeenCalled()`; finally `releaseQr(undefined); await promise.catch(() => undefined);`. Against the pre-fix code (handlers installed only after QR) the current host's shutdown is never called during the QR window, so the assertion fails — this is a true regression guard. Mirror the existing mock harness/exitSpy conventions in that file.

Comment policy: keep the one retained `catch {}` note to a single `//`/inline line; no /* */ essays, no "Phase N".
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npx vitest run tests/headless-gate-bridges.test.ts tests/weixin-command.test.ts</automated>
    <automated>npx @biomejs/biome check src/cli/headless/gate-bridges.ts src/cli/commands/weixin.ts</automated>
  </verify>
  <done>
The bridge queues pendings FIFO and the WR-03 case proves two concurrent asks both resolve to {type:"run_once"}; weixin installs SIGINT/SIGTERM cleanup before runWeixinQrLogin and the WR-04 case proves a QR-window Ctrl-C calls host.shutdown() + exit(0) with channel.start never reached. tsc clean, both listed test files green, biome clean on the two touched source files. Suggested commit: `fix(headless): queue concurrent gate pendings + guard weixin QR-window SIGINT (WR-03, WR-04)`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chat reply text → gate verdict | Untrusted channel message parsed by parse*Choice into a run_command/path_access verdict. Unchanged by this task: parsers are byte-identical (WR-02 word-boundary fail-closed form) and IN-06 only relocates them; existing tampering tests (unmatched → deny) stay green. |
| OS signal → process teardown | SIGINT/SIGTERM during weixin QR login. WR-04 widens graceful-teardown coverage to the QR window (availability/cleanliness, not a new attack surface). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-ne6-01 | Denial of Service | gate-bridges concurrent pauses (WR-03) | medium | mitigate | FIFO pendingQueue so a second parallel gate cannot orphan the first and hang the turn until SIGINT; regression-tested. |
| T-ne6-02 | Denial of Service | weixin QR-login window (WR-04) | low | mitigate | Install SIGINT/SIGTERM cleanup before runWeixinQrLogin so Ctrl-C aborts host cleanly; regression-tested. |
| T-ne6-03 | Elevation of Privilege | gate reply parsing (IN-06 relocation) | low | accept | No logic change — verbatim move of fail-closed word-boundary parsers; deny-default preserved and asserted in gate-bridges + qq-channel-gate-callbacks tests. |

No package-manager installs (npm/pip/cargo) are introduced, so no package-legitimacy gate applies.
</threat_model>

<verification>
- `npx tsc --noEmit` clean across the repo (all interface/signature/import changes type-check, incl. ReviseChoice assignability).
- Targeted vitest green: tests/headless-gate-bridges.test.ts, tests/qq-command.test.ts, tests/telegram-command.test.ts, tests/weixin-command.test.ts, tests/qq-channel-gate-callbacks.test.ts (incl. the new WR-03 + WR-04 cases).
- `npm run lint` (biome) clean; comment-policy test stays green (no new /* */ essays, no "Phase N", any TODO carries #nnn).
- Known pre-existing reds are OUT OF SCOPE and must stay unchanged: tests/ssh-remote.test.ts, tests/ui-mcp-marketplace-snapshot.test.ts, tests/ui-slash-suggestions.test.tsx (per STATE.md 04-02 bucket-3). A full `npx vitest run` will still show exactly those 3 failing — do not "fix" them here.
- Optional full gate before commit: `npm run verify` (build + lint + typecheck + test) — expect the same 3 pre-existing reds and nothing new.
</verification>

<success_criteria>
- IN-01..IN-06 resolved: no dead `effort` field, single bridge-side prompt build via sendPrompt(promptText), optional gateCallbacks (no no-op bags in commands), no `checkpoint` field on the revise verdict, no unused `lowerText`, and parse*Choice/stripFollowupPrefix living only in src/cli/headless/gate-parsers.ts (imported by gate-bridges + 3 hooks).
- WR-03 fixed and regression-tested: two concurrent gate pauses in one turn both resolve.
- WR-04 fixed and regression-tested: Ctrl-C during the weixin QR window runs host.shutdown() and exits cleanly.
- Relevant test surface + tsc + biome all green; the 3 pre-existing reds unchanged; no scope creep into issue #5 (no BaseChannelAdapter, no command-body dedup).
- Three atomic commits on branch `dev` (no Co-Authored-By; v1 untouched).
</success_criteria>

<output>
Update the quick task record on completion. Suggested atomic commits (branch `dev`):
1. `refactor(headless): drop dead effort option, double prompt build, no-op gate callbacks, checkpoint field (IN-01..04)`
2. `refactor(headless): extract shared gate-parsers module, drop unused lowerText (IN-05, IN-06)`
3. `fix(headless): queue concurrent gate pendings + guard weixin QR-window SIGINT (WR-03, WR-04)`
</output>
